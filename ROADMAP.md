# PyExcel — Roadmap to Production

> **Purpose of this file.** The complete, ordered task list to take PyExcel from
> its current state (a debt-laden v1 VBA add-in + a 5%-built v2 .NET skeleton) to
> a single production-grade v2 .NET product. For *where the code lives*, see
> [`ARCHITECTURE.md`](ARCHITECTURE.md).
>
> **How to use it.** Phases are sequential and dependency-ordered. Each phase has
> an objective, deliverables, a task checklist, and exit criteria. A phase is not
> "done" until it also meets the *Definition of production-grade* below.

## Goal & end state

Move everything possible out of v1 VBA into v2 .NET. The Python side shrinks to a
single kernel package (it must stay Python — it runs user `transform()` code).

| Layer | Today | Done |
| --- | --- | --- |
| Orchestration, Excel I/O, UI, setup | ~12k lines VBA | **0 lines VBA** — all .NET |
| Excel ↔ Python IPC | XML files + `meta.xml` polling | named pipe + framing + Arrow |
| Python | `tools.py` + `xmlParsing.py` (~3.7k lines, ~60% dead) | one `pyexcel.kernel` package |
| Distribution | `PyExcel.xlam` | `PyExcel-AddIn64.xll` |

## Status & progress log

**Current position:** Phase 0 ✅ · Phase 1 ✅ · Phase 2 ✅ · Phase 3 ✅ (foundation + `AppEventSink` + CustomXMLPart persistence — code-complete on both COM-coupled tails, awaiting Windows smoke test) · Phase 4 ✅ (every cross-platform checkbox closed: archive-a-run, error surfacing, formula round-trip, UDF cancel bridge, kernel progress, `OnRunPython`, `RibbonRangeParser`, marshalling, dates/nulls/formula tests; the only items remaining under Phase 4's bullet list are explicitly Phase-8-charter WinForms — the modeless progress dialog, per-script error pane, and cell-hover tooltip — listed here for traceability, not for Phase 4 to deliver). **Phase 5 ✅** — every checkbox closed: RFC-4180 `CsvParser` + `CsvWriter` with edge-case tests; CSV/TSV `ImportService` + `ExportService` over `ImportPlanner` / `ExportPlanner` + `CsvCellTypeInference` / `CsvCellFormatter`; **XLSX/XLSM/XLSB import via COM** with `path!Sheet` syntax for sheet selection (defaults to first sheet); `PasteService` over `PastePlanner` (with `ArchivedRun.HasOutput` added to drive selection) **plus `PastePreflight` + WinForms `MessageBox` overwrite confirmation** before destructive writes; `PyExcel.Common.Shell.ShellLauncher` with `OnReadMe` / `OnOpenExplorer` / `OnEditPython` wired; Import/Export/Paste/Import-Output/Export-Output/Paste-Output text fields persisted through `StateService` + `WorkbookStateCodec` (backward-compatible v1 schema); ribbon `OnImport` / `OnExport` / `OnPaste` routed end to end. **Out of scope for Phase 5 (deliberately):** the WinForms sheet picker (Phase 8 dialog) and `.xls` / `.ods` import (planner rejects both with a clean message — neither is in the supported set for v2). See [`docs/phase3-and-4-completion.md`](docs/phase3-and-4-completion.md) for the open Windows smoke tests. **Phase 6 ✅ (code-complete)** — the whole chart pipeline: kernel `chart.py` (Plotly figure → JSON chart spec, Matplotlib figure → SVG/PNG image, both riding `RUN_RESULT` as new Arrow shapes `chart` / `image`), typed `ChartSpec` / `ChartImage` wire values on both sides, cross-platform `ChartSpecParser` + `ChartTraceData` + `ChartColor` (the validated contract), net48 `ChartBuilder` (spec → native chart with orphan-on-failure cleanup; image → embedded picture) wired into `RangeRunner`, explicit unsupported-type errors end to end. The COM render itself is the outstanding Windows + Excel smoke test, same as every other Phase 3–5 COM tail. **Phase 7 ✅ (code-complete)** — new `PyExcel.Setup` project shipping the headless pipeline: cross-TFM `ProcessRunner` in `PyExcel.Common.Shell` (CommandLineToArgvW-rules argv quoter, concurrent stdout/stderr capture, ILog line-forwarding, timeout + cancellation); `KernelResourceExtractor` (kernel `.py` sources + `requirements.txt` shipped as `<EmbeddedResource>` items on `PyExcel.Setup`, extracted at full logical-name path so `import pyexcel.kernel` works against the extraction root, idempotent with byte-equality skip); `SystemPythonProbe` (env-override / PATH-search with explicit Windows-Store-stub detection by reparse-point + file-size heuristic); `VenvProvisioner` (creates `.pyexcel-venv` next to the project matching `PyExcel.Excel.PythonResolver`'s layout, idempotent with corruption recovery); `PipRunner` (`python -m pip install --no-input --disable-pip-version-check`, `Show`, best-effort `UpgradePip` — all streams logged); strict-no-threshold `DependencyVerifier` (per-module `python -c "import X"` against the canonical kernel set); pure `ProjectPathResolver` (UNC long-prefix normalisation, OneDrive/Consumer/Commercial classification with directory-boundary safety); `SetupService` facade orchestrating resolve → ensure-dir → probe → venv → extract → pip-install → verify with per-step typed result entries that never throw past the facade. Working branch: `claude/vigilant-ptolemy-kfggh7`. The runtime side is now wired too: `PyExcel.Excel.PythonResolver.ResolveEmbeddedPath` takes an optional workbook directory and prefers a Setup-extracted `.pyexcel-kernel` package there over the copy bundled beside the `.xll`, mirroring the per-project-venv precedence `ResolvePython` already gives; `KernelHost.Boot` resolves the active workbook directory once and threads it through both resolvers (which also lights up the previously-dead venv tier). **Open Phase 7 items:** the first-run wizard UI (deferred to Phase 8 WinForms) and retiring `Update.bas` (sliced into Phase 9 alongside the rest of `src/module/` removal) — both deferred out of Phase 7, so the phase's own engineering is code-complete pending the Windows fresh-machine smoke test. **Phase 8 code-complete (pending Windows smoke test)** — `PyExcel.Forms` now ships all nine dialogs plus the embedded ribbon logo. EditAction, sheet picker, Edit-Import/Export/Paste, **range picker** (embedded as a "Pick…" button on every range field in EditAction + EditIo, with "Use current selection" reading the live selection), **orientation** (1-D-list paste direction, wired into `PasteService`), **export wizard** (multi-row range→file batch with working Add/Remove — the v1 dead-button bug fixed by a `FlowLayoutPanel` — run via `ExportService.RunBatch`, new ribbon button), and the **progress dialog** (modeless, working Cancel → kernel `CANCEL`; closes the long-deferred Phase 4 #5). The off-screen `(-20000,-20000)` hide hack never appears (every form is modal/modeless with an owner), and the `customLogo.png` ships as an embedded resource decoded by `PyExcelRibbon.LoadImage`. Every COM/WinForms surface awaits the Windows + Excel smoke test, same as the rest of the COM tails.

Terse running record (newest first) so a new session can pick up where work stopped:

- **2026-06-14 — Make enablement durable via a reliable local store (workbook stays a PyExcel project across reopen).** The CustomXMLPart-in-the-workbook round-trip still left re-opened workbooks asking to be enabled again (it only persists if the user saves, `BeforeSave` fires, and the part survives the write — and it leans on the COM event sink, which has never had a Windows smoke test). New `LocalStateStore` (in `PyExcel.State`, cross-platform) persists each workbook's state — enabled flag, project dir, actions, field bindings — to a per-user file under `%LOCALAPPDATA%\PyExcel\workbooks\<sha256(path)>.xml`, keyed by the workbook's on-disk path (unsaved workbooks are skipped — no stable key yet). It's written **the instant** the user enables a workbook or edits an action (ribbon `PersistLocal`), plus on save/close, so it never depends on Excel deciding the workbook is "dirty". Restore is now answered three ways, so an enabled workbook comes back enabled no matter which path runs: the event sink's `EnsureRestored` on `WorkbookOpen` **and** `WorkbookActivate` (load-if-empty: local store → CustomXMLPart → v1 legacy, never clobbering a workbook being edited), the `AutoOpen` already-open scan, and — crucially, independent of any COM event firing — an **on-demand restore in the ribbon's `ActiveState`**: every render asks "is this workbook a saved project?" and restores from the local store if memory is empty (with a per-key negative cache so it hits disk at most once per never-enabled workbook). The CustomXMLPart stays as the portable copy that travels with the file to another machine. Tests: 4 `LocalStateStoreTests` (round-trip enabled/projectdir/actions, unsaved-key skip, unknown-key null, remove). Still the Windows + Excel smoke test for the COM tails.
- **2026-06-14 — Persistence + activation robustness + Read Me button (live-test fixes).** Three issues from running the add-in. **(1) Read Me did nothing** — nothing ever created a README, so `OnReadMe` always fell through to the "not found" alert. `ProjectScaffolder` now writes a `README.md` at the project root on Enable (never overwriting a user-edited one), and `OnReadMe` looks there *and* in the workbook folder, creating a default README on demand if a pre-existing project has none — so the button always opens something. **(2) Not persistent (re-enable required on reopen)** — state was only restored in the `WorkbookOpen` event, but on a double-click / command-line launch Excel often opens the workbook *before* the `.xll`'s `AutoOpen` subscribes the event sink, so that event is missed and the saved enabled-flag/actions never load. `AppEventSink.RestoreOpenWorkbooks()` (called from `AutoOpen` right after wiring) now restores every already-open workbook. Also: PyExcel state lives outside the cell grid, so adding an action / enabling didn't dirty the workbook — Ctrl+S could be a silent no-op that never wrote the `CustomXMLPart`. The ribbon now calls `MarkWorkbookDirty()` (`ActiveWorkbook.Saved = false`) after Enable / Add / Edit / Delete action so a save actually flushes state. **(3) Ribbon greys out on focus changes (file dialogs, etc.) and doesn't recover** — `ExcelWorkbookContext.CurrentWorkbookKey` did a live `Application.ActiveWorkbook` COM call on every ribbon repaint; when Excel is busy that throws and it returned null, grey-ing the whole ribbon with nothing to re-enable it. It now caches the last good key/dir and falls back to the cache on a transient COM fault (a genuinely-closed workbook still renders disabled because the sink Forgets its state). Tests: 2 new `ProjectScaffolderTests` (README written / not overwritten). No local .NET SDK / Windows, so the COM event timing and the ribbon render are the Excel smoke test.
- **2026-06-14 — Fix WinForms cramming/overlap on high-DPI & smaller laptop screens (manual DPI scaling).** Every dialog is laid out in code at absolute 96-DPI pixel coordinates, so on a higher-DPI / smaller screen the system font renders larger but the fixed coordinates don't — rows collide, text overlaps, the window doesn't fit. **Two earlier attempts via WinForms' own auto-scaling failed**: `AutoScaleMode.Dpi` reads `Control.DeviceDpi`, which stays 96 unless the host opts into WinForms per-monitor DPI (an Excel `.xll` doesn't), and `AutoScaleMode.Font` likewise measured 96 in that hosted context — both scaled by 1.0 and did nothing. Final fix: `ScaledForm` base (in `PyExcel.Forms`, `#if NETFRAMEWORK`) now turns WinForms auto-scaling **off** and scales the layout **itself** in `OnLoad` from the real monitor DPI reported by Win32 `GetDpiForWindow` (which honours the host's DPI awareness and can't get stuck at 96): it walks the whole control tree multiplying every control's bounds by `dpi/96` (fonts left alone — point sizes already render at device DPI, so only positions/box sizes need to grow), scales `ClientSize`/`MinimumSize`/`MaximumSize`, then caps the window to the screen working area with `AutoScroll` so it always fits. Exposes `DpiScaleFactor` + `ScaleNewControl` so dynamically-added content scales too (wired into `ExportWizardForm.AddRow`). All ten dialogs derive from `ScaledForm`. No local .NET SDK / Windows in the web env, so on-screen rendering is the Excel smoke test. *Follow-up (live test):* absolute-positioned dialogs scaled cleanly, but the **anchored** ones (`SetupForm`, `ErrorDisplayForm` — big text box anchored to all edges, buttons anchored bottom-right) were still broken: scaling a Bottom/Right-anchored control while the form is still at its small design size puts it outside the client area, so the layout engine flings it off-screen (the Setup Close button vanished; the log box was distorted). Fixed by detaching all non-`Top|Left` anchors before the scale and restoring them against the scaled bounds afterwards, and by dropping `AutoScroll` from the fit-to-screen cap (it fought the bottom-anchored buttons) — the all-edge-anchored text box now absorbs the cap while the button bar stays visible.
- **2026-06-12 — Live-testing fixes: surface Python `print()` output, make "Show Last Error" actually open.** Two bugs from running the v2 add-in. **(1) Run prints went nowhere.** The kernel subprocess is spawned with `RedirectStandardOutput`/`RedirectStandardError` (so child stdio can't corrupt the separate framing pipe), but nothing drained those streams during normal operation — so user `print()` / `logging` output sat invisibly in the OS pipe buffer (and risked stalling the child once the buffer filled). `KernelSupervisor` now starts two background reader threads in its constructor that drain stdout/stderr line-by-line and raise a new public `OutputReceived` event (`KernelOutputEventArgs { IsError, Text }`); `PYTHONUNBUFFERED=1` (already set) means lines stream live. `KernelHost.Boot` (net48) forwards `OutputReceived` — and the previously-unsubscribed `KernelClient.LogReceived` — into Excel-DNA's `LogDisplay`, so prints show up in the log window as `[python] …` / `[python:stderr] …`. **(2) "Show Last Error" did nothing.** It relied on `LogDisplay.Show()`, which is a no-op once the log window is already open behind Excel (and never raises it to the foreground) — so once any prior message had popped the window, the button silently failed. `OnShowLastError` now opens a new modal, Excel-owned, resizable read-only `ErrorDisplayForm` (monospaced, with a Copy button) so the formatted traceback always comes to the front. **Tests:** 2 `KernelClientTests` against a real kernel — `print()` to stdout surfaces via `OutputReceived` (IsError=false), a `sys.stderr` write surfaces with IsError=true. No local .NET SDK in the web env, so the WinForms dialog + the LogDisplay forwarding are the Windows smoke test. *Follow-up:* a failed Run-button run now also pops that modal `ErrorDisplayForm` ("PyExcel — Run failed") so a Python traceback stays on screen until dismissed instead of scrolling past in the log window — `RangeRunner.RunActiveScript` takes an optional `errorDisplay` sink (injected by the ribbon, same pattern as `progressFactory`) invoked on the main thread for kernel/host failures and output-write faults.
- **2026-06-12 — Script names keep the `.py` extension (fixes Edit-script "file not found").** The watcher/`Snapshot` stripped the extension (`GetFileNameWithoutExtension`), but `ScriptScaffold` (the "New…" button) and the action form's Script combo use full filenames, and `OnEditPython` opens `<projectDir>/userScripts/<name>` — so a stripped "example" couldn't be found on Edit, and the form's `existing.Script` / `SelectedItem` never matched the list. Switched both watcher paths to `Path.GetFileName` (keep `.py`); `AvailableScripts`, the ribbon dropdown, and the form now agree on the on-disk filename and Edit opens it. Also fixed the matching latent run path: `RangeRunner` resolved the script against the workbook folder (`<wbDir>/example`, missing the `userScripts/` subfolder and extension) — it now resolves `<projectDir>/userScripts/<file>.py` (same project-dir rule as Edit / KernelHost) and hands the kernel the absolute path. Watcher tests updated to expect `.py`.
- **2026-06-12 — Script/Input/Output boxes are display-only.** Per request: only the Actions dropdown is interactive; the Script combo and the Input/Output editBoxes mirror the selected action and reject hand-edits. Their onChange callbacks (`OnScriptChange`/`OnPyInputChange`/`OnPyOutputChange`) no longer store — they `RevertControl` (full `QueueInvalidate`) so `getText` restores the behind-the-scenes value. The controls stay `getEnabled` (normal look, not greyed) and there's no message box. `StateService.SetSelectedScript`/`SetPyInput`/`SetPyOutput` remain as tested API but are now driven only via `LoadAction`.
- **2026-06-12 — Live Script watcher.** Promoted script-list population from one-shot scans to a live `ScriptDirectoryWatcher` owned by `AppEventSink`: (re)pointed at the active workbook's userScripts folder on `WorkbookOpen`/`WorkbookActivate` (and disposed on add-in unload), so a `.py` added/removed/renamed on disk updates the ribbon's Script dropdown with no re-activate. The watcher's worker-thread callback marshals the `SetAvailableScripts` update onto Excel's macro thread (`ExcelAsyncUtil.QueueAsMacro`), since the resulting `StateChanged` drives the ribbon's COM-touching `OnStateChanged`. Enable starts the watcher immediately through a new `PyExcelServices.RequestScriptRefresh` hook (Enable provisions the folder but fires no `WorkbookActivate`). One-shot `Snapshot` stays the fallback when the folder doesn't exist yet (not-yet-Enabled / unsaved / cloud workbook). CI/Windows-verified only.
- **2026-06-12 — Wire the ribbon together: Script list populated, selecting/adding an action loads the run boxes.** From live testing — "nothing is connected and synced." Two real disconnects (the invalidation path itself was fine: `RibbonOnLoad`→`StateChanged`→`QueueAsMacro(Invalidate)`). **(1) `AvailableScripts` was never populated in-process** — `ScriptDirectoryWatcher` was only ever constructed in tests — so every Script list (ribbon `cmbScript` *and* the Add/Edit form) was empty. Added `ScriptDirectoryWatcher.Snapshot(dir)` (one-shot scan) and call it on Enable, before opening the action form (`RefreshAvailableScripts`), and on `WorkbookOpen`/`WorkbookActivate` (`AppEventSink.RefreshScripts`, resolving the same project dir the ribbon/KernelHost use). *(Scan-based, not live: a `.py` added externally shows after a workbook switch or reopening the form — live watching is a later enhancement.)* **(2) `OnActionChange` only set `SelectedActionName`** — picking a saved action showed its name in the combo but left the Script/Input/Output boxes (which is what `RangeRunner.RunActiveScript` actually reads) untouched. Added `StateService.LoadAction` (atomic script+input+output+select) and call it from `OnActionChange` and after Add/Edit, so selecting or saving an action loads it into the run boxes. Tests: `LoadAction`, `Snapshot`. CI/Windows-verified only.
- **2026-06-12 — On-Excel testing fixes: dedicated project directory, Setup scaffolds folders, range picker raised.** From first live runs of the v2 add-in. **(a) Setup builds the user-facing folders** — new `ProjectScaffolder` (a `scaffold-project` `SetupService` stage, run before the Python stages) creates `<projectDir>/userScripts` + a starter `example.py`; previously Setup only made `.pyexcel-venv`/`.pyexcel-kernel`, so "the scripts and folders" never appeared. **(b) Dedicated project directory (pick once, remembered)** — Enable now opens a folder browser (default = the workbook's resolved dir); the chosen folder is saved on the workbook (`WorkbookState.ProjectDir`, persisted by the codec at schema v1 via a new optional `<project-dir>` element) and preferred by both Setup and the runtime (`KernelHost`) plus every ribbon userScripts lookup (`ResolveProjectDir` — used by Enable/Edit/New/Open-In-Explorer). `PyExcel.State` can't see `PyExcel.Common`, so the "stored ?? `ProjectDirectory.Resolve(workbookDir)`" rule is intentionally duplicated in `KernelHost` and the ribbon. **(c) Range picker raised above Excel** — `SetForegroundWindow(ExcelDnaUtil.WindowHandle)` before `Application.InputBox`. Tests: `ProjectScaffolderTests`, codec round-trip for `ProjectDir`, `SetupServiceTests` step-order updated. Open items: `ScriptDirectoryWatcher` still isn't started in production (the Script dropdown isn't fed from disk yet — separate Phase 3/4 gap); and "Setup builds nothing" on the user's box is most likely a stage failure (e.g. no Python on PATH) — pending their Setup log. No local .NET SDK, so CI/Windows-verified only. *Follow-up:* the folder browser now opens at the workbook's own folder (`browseStart = Workbook.Path`), and enabling a **never-saved** workbook prompts for a name (`PromptForWorkbookName`, Excel `InputBox` text) then `SaveActiveWorkbookAs`-es it (`.xlsx`, format 51) into the chosen folder and re-keys off the saved path before Setup runs.
- **2026-06-11 — UI fixes (Notes 1–4): native range picker, Enable=install, list-based action ranges, new-script scaffold.** Four usability fixes to the running v2 add-in, on top of the v1 removal. **(1) Native range picker** — the dialogs now pick ranges with Excel's own selector (`Application.InputBox` Type:=8) via `PyExcelRibbon.PickRangeNative`; `RangePick.OnSheet` hides the dialog while the user selects on the sheet, replacing the old button that only echoed the prior selection. Wired into Import/Export/Paste (`EditIoForm`) and the Action dialog. **(2) New-script scaffold** — the EditAction Script field gains a "New…" button that creates `userScripts/<name>.py` from a `transform()` template (`ScriptScaffold` — cross-platform, tested), so a fresh workbook with no scripts isn't a dead end. **(3) Enable = install** — `OnEnablePyExcel` now runs the full Setup (folders/venv/kernel/deps via `SetupForm`) and marks the workbook enabled in one action; the separate Setup button is removed; the button greys once enabled (`RibbonNotEnabled`). Update is a disabled placeholder (`RibbonUpdateAvailable` == false) with the open item (version compare + launch-time update check) documented under Phase 9. **(4) List-based action ranges** — EditAction's input/output are now `RangeListEditor` lists (add/edit/remove/up/down) whose rows are picked + optionally named via `RangeNameForm`, serialised back to the ribbon `{name}=range; …` string via the new `RibbonRangeParser.Format` (round-trip tested). `RangePickerForm` retired. **Tests:** `RibbonRangeFormatTests` + `ScriptScaffoldTests` on the cross-platform lane. No local .NET SDK in the web env, so the WinForms shells + the native InputBox/hide-restore are the Windows smoke test. Working branch: `claude/jolly-dirac-qiytsw`.
- **2026-06-11 — Phase 9: v1 (`.xlam`) removed from the repository.** Deleted the v1 VBA add-in and its assets — `src/module/` (all `.bas`/`.cls`/`.frm`/`.frx`), `src/embedded/` (v1 `tools.py` / `xmlParsing.py` / `requirements.txt` / `instructions.txt` / `Uninstall.txt`), `src/Ribbon/`, and the compiled `PyExcel.xlam`. The one remaining v2→v1 coupling was the ribbon logo: `PyExcel.Ribbon` embedded `..\Ribbon\customLogo.png`, so the PNG was relocated into `src/PyExcel.Ribbon/Resources/` (LogicalName unchanged, so `LoadImage("customLogo")` still resolves) and the `.csproj` `<EmbeddedResource>` repointed before `src/Ribbon/` was removed. Verified no other build/CI reference to the deleted paths — root `requirements.txt` (the slimmed kernel set `PyExcel.Setup` embeds) is untouched, and the v2 kernel lives in the top-level `embedded/`, not the deleted `src/embedded/`. Docs made consistent with the deletion: `README.md` (now a v2 overview, no `.xlam` install steps), `ARCHITECTURE.md` (dropped the "two codebases" framing → a single .NET codebase plus a short history note; removed deleted-file map/navigation entries), and this file. Provenance comments in the v2 C# that name v1 files (e.g. "port of `chartBuilder.bas`") were left as lineage notes. The repository now contains no VBA. Working branch: `claude/jolly-dirac-qiytsw`.
- **2026-06-11 — Phase 9 start: v1→v2 state migration converter (pure core).** Opened Phase 9 (cutover) with the one cross-platform, testable deliverable in it — the v1→v2 per-workbook state migration (Decision #3). Deliberately *not* touching the destructive/Windows-bound Phase-9 items yet (deleting `src/module/` etc., Excel QA, dropping `-alpha`): v2 hasn't passed a single Windows + Excel smoke test, so retiring the only working product now would be premature. **`LegacyStateConverter`** (in `PyExcel.State`, next to `WorkbookStateCodec`) is the COM-free core: given the raw v1 defined-Name string values captured for one sheet (a `LegacyWorkbookState` record), it produces the equivalent `WorkbookState` the codec serializes into a `CustomXMLPart`. The exact v1 Name keys are pinned in a nested `LegacyNames` class as the single source of truth (`PyExcelEnabled` workbook-scoped; `SelectedAction`/`Actions`/`cmbScript`/`txtPyInput`/`txtPyOutput`/`txtImportInput`/`txtImportOutput`/`txtExportInput`/`txtExportOutput`/`txtPasteOutput` sheet-scoped). **`ParseActions`** is a faithful port of v1's `LoadActionsForSheet`: Chr(29)→Chr(10)→`;` separator auto-detection (so it reads data written by any earlier v1 build), pipe-delimited fields, named `key=value` format vs legacy positional `name|script|input|output|…`, repeated `input=`/`output=` accumulating with `"; "` (matching v1's writer), split-on-first-`=` so a value may contain `=`, blank/malformed rows skipped, duplicate names keep the first (v2 treats names as a unique upsert key), and v1's `entireRow`/`entreToEnd` flags dropped (row-spill is designed out of v2). Scalar text fields are trimmed and blank→null (so the codec omits them); the enabled flag parses v1's `"1"`/`"0"` plus the textual booleans. **Per-sheet→per-workbook gap** is left to the (Windows) COM driver by design — v1 stored independent state per sheet, v2 is workbook-scoped, and there's no lossless multi-sheet collapse, so the driver picks which sheet to carry forward. **Tests:** 24 `LegacyStateConverterTests` (13 `ParseActions`: null/empty, named single/multi, repeated input/output accumulation, legacy positional, Chr(10)/`;` fallbacks, entireRow/entreToEnd dropped, duplicate-first, blank-row skip, value-with-`=`, malformed-row skip, trim; 11 `Convert`: full-field mapping, blank→null, enabled-encoding theory, scalar trim, empty-legacy≡Empty, null-arg guards, and an end-to-end migration round-trip through `WorkbookStateCodec.SerializeToString`→`TryDeserialize`) — all cross-platform on the Linux net8.0 lane. The COM read of the live workbook's Names + the CustomXMLPart write are the Windows smoke test, same split as every other phase. Working branch: `claude/clever-wozniak-egaoca`.
- **2026-06-11 — Phase 8 finished: range picker, orientation, export wizard, progress dialog + embedded ribbon logo.** The last four dialogs plus the logo, completing the nine-dialog Phase 8 set. **Range picker** — cross-platform `RangeAddressValidator` (a picked range is a *single* reference, validated through `RibbonRangeParser`, rejecting the `;`/`name=` multi-binding syntax) + net48 `RangePickerForm` (a box + "Use current selection" button fed by an injected `Func<string?>` that reads `Application.Selection` late-bound, so the form takes no COM dependency). Embedded as a "Pick…" button on every range field — `EditIoForm` (range-kind rows) and `EditActionForm` (input/output) gained an optional `selectionProvider`; the ribbon supplies `CurrentSelectionAddress`. **Orientation** — cross-platform `ListOrientation` enum + `OrientationResolver.Resolve(rows, cols)` (the v1 paste-direction rule: a multi-cell target dictates direction by shape, a single cell is ambiguous ⇒ ask) + net48 `OrientationForm` (Across/Down). Wired into `PasteService.RunActivePaste` via an optional `orientationChooser`: a 1-D vector into a single cell prompts; Vertical reshapes the vector to an N×1 column so the existing table-write path handles it; null chooser preserves the old row-spill behaviour. **Export wizard** — cross-platform `ExportJob` + `ExportBatchValidator` (per-row reuse of `ExportPlanner`, duplicate-target guard, 1-based row errors) + net48 `ExportWizardForm` (rows in a `FlowLayoutPanel` so Add/Remove reflow correctly — the v1 dead-row-button bug is fixed by construction; Browse via `SaveFileDialog`) + `ExportService.RunBatch` (reads every source on the main thread, writes each file on a background task) + new ribbon **Wizard** button (`OnExportWizard`, seeded from the single-export fields). **Progress** — cross-platform `IRunProgressSink` + `ProgressModel` (clamp/format) + net48 modeless `ProgressForm` (label + bar + Cancel; marquee for indeterminate). `RangeRunner.RunActiveScript` gained an optional `progressFactory`: when supplied it subscribes to `KernelClient.ProgressReceived`, runs via the cancellable `ExecuteManyAsync` with the form's token (Cancel → `CANCEL` frame), and tears the dialog down in a `finally`; null preserves the original synchronous path. Closes the long-deferred Phase 4 #5. **Logo** — `src/Ribbon/customLogo.png` ships as an `<EmbeddedResource LogicalName="customLogo.png">` on `PyExcel.Ribbon`; `PyExcelRibbon.LoadImage("customLogo")` decodes it to a `Bitmap` (cloned off the stream), with `System.Drawing` referenced. **Wiring:** `PyExcel.Forms` already referenced `PyExcel.Excel`; ribbon callbacks `OnRunPython` / `OnPaste` / `OnAddAction` / `OnEditAction` / `OnEditImport` / `OnEditExport` / `OnEditPaste` / `OnExportWizard` all thread the new delegates; `RibbonUI.xml` gains the Wizard button. **Tests:** 6 `RangeAddressValidatorTests` + 8 `OrientationResolverTests` + 6 `ExportBatchValidatorTests` + 8 `ProgressModelTests` — all cross-platform on the Linux lane. Every WinForms form, the COM selection read, the orientation/paste reshape, the batch export, and the live progress/cancel run are the Windows + Excel smoke test. Working branch: `claude/zealous-hypatia-csvf94`.
- **2026-06-11 — Phase 8: Edit-Import / Edit-Export / Edit-Paste dialogs (one parameterised form), ribbon wired.** The third Phase 8 slice — the guided two-field editors for the same Import / Export / Paste workbook-state fields the ribbon edit-boxes hold. **`EditIoValidator`** (cross-platform, in `PyExcel.Excel` next to the planners) validates each by *reusing the run-time planner* as the validity check — `ValidateImport` / `ValidateExport` call `ImportPlanner.Create` / `ExportPlanner.Create` and surface their `FormatException` (blank field, unsupported `.xls`/`.ods`, Excel-format export target) / `ArgumentException` (invalid path) verbatim, then return the user's *trimmed* original strings to persist (not the planner's resolved absolute path); `ValidatePaste` just requires a non-blank target range (Paste pulls its data from the last archived run, not a user field). Returns an `EditIoValidationResult` (`IsValid` / `ErrorMessage` / `Input` / `Output`) so the dialog shows the message inline and only a valid result closes — the same "caught in the dialog, not downstream" contract as EditAction. **`EditIoForm`** (net48 WinForms in `PyExcel.Forms`) unifies all three into one parameterised two-field editor (they differ only in labels and which field browses for a file): a row is a plain range box or a box + **Browse…** button opening an `OpenFileDialog` (import source — CSV/TSV/TXT/XLSX/XLSM/XLSB filter) or `SaveFileDialog` (export target — CSV/TSV; `OverwritePrompt=false` since the export service runs its own overwrite confirmation); Paste shows only the target-range row. Static `PromptImport` / `PromptExport` / `PromptPaste` each build the form + bind the matching validator (capturing the workbook directory) and return the validated result or null on cancel; shown modally owned by Excel's window. **`PyExcel.Forms` now references `PyExcel.Excel`** so the forms reach `EditIoValidator` (natural UI→domain direction; both multi-target the same TFMs, no cycle since Excel doesn't reference Forms). **Ribbon:** the formerly-stubbed `OnEditImport` / `OnEditExport` / `OnEditPaste` now show the form pre-populated from `ActiveState()` and persist the result through `StateService.SetImportInput/Output` / `SetExportInput/Output` / `SetPasteOutput`, passing `WorkbookContext.CurrentWorkbookDirectory` for path resolution. **Tests:** 18 `EditIoValidatorTests` (import: valid CSV, valid xlsx+sheet, trim, blank input/output, `.xls`/`.ods` reject; export: valid, trim, blank input/output, `.xlsx`/`.xls`/`.ods` target reject; paste: valid, trim, blank reject) — cross-platform on the Linux lane. The WinForms form + the file dialogs are the Windows smoke test. Working branch: `claude/zealous-hypatia-csvf94`.
- **2026-06-11 — Phase 8: sheet picker dialog, wired into Excel import (closes the Phase 5 deferral).** Second Phase 8 dialog. **`SheetPickerForm`** (net48 WinForms in `PyExcel.Forms`) is the port of v1's `SheetPickerForm`: a drop-down of the workbook's sheet names + OK/Cancel, shown modally owned by Excel's window, with a static `Prompt(owner, sheets, preselected) → string?` (null on cancel). **`SheetPickerValidator`** (cross-platform) is its selection rule — a sheet must be chosen and must be one the workbook offers; membership is case-insensitive (Excel sheet names are unique that way and `Sheets[name]` looks up case-insensitively) and the canonical workbook casing is returned so the downstream COM lookup matches. **`SheetSelection.Resolve`** (cross-platform, in `PyExcel.Excel`) is the prompt-or-resolve decision: a pinned `!Sheet` always wins (`Resolved`); no pin + one sheet auto-resolves; no pin + several → `Prompt`; no sheets → `Empty`. **`ImportPlanner.Compose(path, sheet?)`** (cross-platform) is the inverse of `ParsePathAndSheet` — builds `path!Sheet`, blank sheet returns the path unchanged, a sheet on a non-Excel path is rejected; round-trip-tested against `ParsePathAndSheet`. **Wiring:** `ImportService.RunActiveImport` gained an optional `Func<IReadOnlyList<string>, string?>? sheetChooser`; the Excel read path now enumerates the workbook's worksheet names (`EnumerateSheetNames`, skipping chart sheets), runs `SheetSelection.Resolve`, and on the `Prompt` branch invokes the chooser (null chooser → first sheet, preserving the pre-picker default; chooser returns null → import cancels cleanly, `ReadExcelSource` returns null which `RunExcelImport` treats as a quiet abort). The COM-bound service stays free of a WinForms-project dependency — the chooser is injected; ribbon `OnImport` supplies `sheets => SheetPickerForm.Prompt(ExcelWindowOwner(), sheets, null)`, which runs on the macro thread where the workbook is open. **Tests:** 6 `SheetPickerValidatorTests` (null-sheets throws, blank selection fails, not-in-workbook fails, exact match, case-insensitive → canonical casing, trim) + 8 `SheetSelectionTests` (null throws, pinned-wins-regardless-of-count, pinned trimmed, no-pin single → resolved, no-pin multiple → prompt with list, no-pin empty → Empty, pinned-against-empty still resolves) + 11 new `ImportPlannerTests.Compose` cases (null path throws, blank sheet unchanged, append, trim, sheet-on-csv/tsv/txt/no-ext throws, round-trips for xlsx/xlsm/xlsb + space-in-name) — all cross-platform on the Linux lane. The WinForms form + the COM enumerate/open/read remain the Windows + Excel smoke test. Working branch: `claude/zealous-hypatia-csvf94`.
- **2026-06-11 — Phase 8 start: `PyExcel.Forms` project + EditAction dialog, ribbon Add/Edit wired.** Opened Phase 8 (Forms & UI polish) with the WinForms dialog layer and its first form. New **`PyExcel.Forms`** project multi-targets `net48;netstandard2.0` exactly like `PyExcel.Setup` / `PyExcel.Excel`: the WinForms forms are `#if NETFRAMEWORK` so the netstandard2.0 build (Linux CI) compiles only the cross-platform validation logic the dialogs delegate to. **`EditActionValidator`** (cross-platform, pure) validates the Add/Edit-action fields and, on success, builds the slim v2 `RibbonAction` (name + script + input + output + optional kwargs): name required + ordinal-unique (matching `StateService.AddAction`'s upsert key) with an `originalName` carve-out so an in-place edit that keeps the name isn't flagged, script/input/output required, all fields trimmed, empty kwargs normalised to `null`, returns an `EditActionValidationResult` (`IsValid` / `ErrorMessage` / `Action`) rather than throwing so the dialog shows the message inline and only a valid result closes — the Phase 8 "invalid input caught in the dialog, not downstream" exit criterion. **`KwargsText`** (cross-platform) round-trips the optional kwargs between the dictionary and the one-`name=value`-per-line text the form edits: tolerant of all three line endings, splits on the first `=` (so a value may contain `=`), trims keys/values, ignores blank lines, rejects a missing `=` / blank key / duplicate key with a reason; `Format` is the inverse. **`EditActionForm`** (net48-only WinForms) is the thin shell — name box, script dropdown (populated from `AvailableScripts`), input/output boxes, a multiline kwargs box, an inline error label, Save/Cancel — shown modally with Excel's main window as owner (`ExcelDnaUtil.WindowHandle` wrapped in an `IWin32Window`), so the v1 off-screen `(-20000,-20000)` hide hack is gone; Save runs `KwargsText.TryParse` then `EditActionValidator.Validate` and only closes on success. **Ribbon wiring:** the formerly-stubbed `OnAddAction` / `OnEditAction` now show the form and route the result through `StateService.AddAction` (Add: upsert; Edit: when the name changed, `DeleteAction` the old entry first since `AddAction` upserts by name, so a rename would otherwise orphan the original). **Wiring:** `PyExcel.sln` gains the project at GUID `…A0A`; `PyExcel.Ribbon.csproj` adds the `ProjectReference` (the assembly flows into the `.xll` transitively through Addin → Ribbon); `PyExcel.Bridge.Tests.csproj` references it for the tests; CI's Linux lane gains a `Build PyExcel.Forms (netstandard2.0)` step. **Tests:** 16 `KwargsTextTests` (blank → empty, single/multi pair, all line endings, blank-line skip, value-with-`=`, trim, empty value, missing-`=`/blank-key/duplicate-key failures, case-sensitive keys, `Format` null/empty/pairs, round-trip) + 19 `EditActionValidatorTests` (happy path, trim-all, kwargs copied + not aliased, empty-kwargs → null, each required field blank, duplicate-name in Add mode + after-trim, edit-keeps-name allowed, rename-onto-another fails, rename-to-fresh allowed, case-sensitive collision, null-existing-names throws) — cross-platform, run on the net8.0 Linux test host. The WinForms form + ribbon COM wiring aren't unit-testable on Linux (no SDK in the web env; verified on CI) — the actual modal render is the Windows + Excel smoke test for this slice, same as every other COM/UI tail. Working branch: `claude/zealous-hypatia-csvf94`.
- **2026-06-11 — Phase 7 runtime wiring: `PythonResolver.ResolveEmbeddedPath` consults the Setup-extracted `.pyexcel-kernel`.** Closed the last non-deferred Phase 7 item — the bridge between the headless Setup engine (which extracts the kernel into `<project>/.pyexcel-kernel/pyexcel/…`) and the runtime that has to `import pyexcel.kernel`. **`PyExcel.Excel.PythonResolver.ResolveEmbeddedPath`** gained an optional `string? workbookDir` parameter and a two-tier resolution order mirroring `ResolvePython`'s venv-before-PATH precedence: (1) when a workbook directory is supplied and `<workbookDir>/.pyexcel-kernel/pyexcel/kernel/__main__.py` exists, return that `.pyexcel-kernel` directory (the `PYTHONPATH` root the `KernelResourceExtractor` writes under); (2) otherwise fall back to the bundled `embedded/` found by walking up from `AppContext.BaseDirectory` for the same `pyexcel/kernel/__main__.py` marker — the copy that ships beside the `.xll` (and the repo-root `embedded/` the integration tests use). New `PythonResolver.ExtractedKernelDirName` const (`.pyexcel-kernel`) documents the agreement with the literal in `SetupService` so the two halves can't silently drift. The bundled walk-up and the new extracted-kernel check now share one `HasKernelPackage(root)` marker helper (keyed on `root/pyexcel/kernel/__main__.py`), so there's a single source of truth for "is there an importable kernel here". The thrown `DirectoryNotFoundException` message now names both miss reasons (no `.pyexcel-kernel` under the workbook, no walked-up `embedded/`). **`KernelHost.Boot`** now reads `PyExcelServices.WorkbookContext.CurrentWorkbookDirectory` once and threads it through *both* `ResolvePython(workbookDir)` and `ResolveEmbeddedPath(workbookDir)` — which, as a bonus, lights up the per-project `.pyexcel-venv` tier that was previously dead because `Boot` passed no directory. Null (unsaved/no active workbook) falls back to the bundled defaults exactly as before; the process-wide lazy boot captures the workbook active at first kernel use, which is acceptable for the documented Phase-4 stop-gap host. **Tests:** new `PythonResolverTests` (6 cases — no-workbook-dir falls back to bundled `embedded/`; extracted `.pyexcel-kernel` present is preferred; workbook dir without an extraction falls back; a partial/interrupted extraction with the `.pyexcel-kernel` dir but no marker is *not* matched and falls back; null/empty/whitespace workbook dir is ignored and falls back). netstandard2.0-clean (only `Path`/`File`/`DirectoryInfo`/`AppContext`), so it runs on the net8.0 test host on the Linux lane. C# build + suite verified on CI (no local .NET SDK in the web environment). With the wizard UI deferred to Phase 8 and `Update.bas` retirement sliced into Phase 9, Phase 7's own engineering is code-complete pending the Windows fresh-machine smoke test.
- **2026-06-10 — Phase 7 headless half code-complete: `PyExcel.Setup` (kernel resource extraction, venv, pip, dep verification, path resolver).** New `PyExcel.Setup` project (multi-target `net48;netstandard2.0`, references `PyExcel.Common`, wired into the .sln + `PyExcel.Addin` + `PyExcel.Bridge.Tests` + the Linux CI build matrix) carrying the headless half of Phase 7 — every stage that doesn't need WinForms. **`PyExcel.Common.Shell.ProcessRunner`** (the cross-TFM child-process helper Setup leans on; lives next to `ShellLauncher` since they're two halves of the same shell-invocation surface — `ShellLauncher` opens files with registered handlers via `UseShellExecute=true`, `ProcessRunner` runs an explicit argv with `UseShellExecute=false` and captures both streams). Argv quoting follows the documented CommandLineToArgvW round-trip rules so a path with spaces, embedded `"`, or trailing `\` survives parsing on the child side — no `ProcessStartInfo.ArgumentList` because that's netstandard2.1+ and we target 2.0. Stdout/stderr are read concurrently via `BeginOutputReadLine`/`BeginErrorReadLine` to avoid the classic pipe-buffer deadlock, each line forwarded to `ILog.Info`/`Warn` as it arrives so an operator tailing `%TEMP%\PyExcel_Debug.log` watches wheels download in real time; the full accumulated text comes back on `ProcessRunResult.{Stdout,Stderr}`. Timeout and `CancellationToken` both kill the child and follow with a no-arg `WaitForExit()` so the async reader handlers flush their last frame before the result is returned — the bounded-timeout overload alone doesn't drain them. **`KernelResourceExtractor`** writes the kernel onto disk from .NET embedded resources — replacing v1's `Setup.bas` base64-on-a-hidden-sheet plus 32 000-char chunk-assembly with a plain `<EmbeddedResource>` per `.py` file in `PyExcel.Setup.csproj`. `LogicalName` is the on-disk relative path (`pyexcel/kernel/__main__.py` etc., slash-separated) so the round-trip back to a directory tree is mechanical — the extractor does NOT strip the `pyexcel/` prefix because the package directory must exist on disk for `import pyexcel.kernel` to resolve once `<target>` is on PYTHONPATH. Idempotent: a file present with identical bytes is skipped; tampered or stale bytes are overwritten; the result lists Written + Skipped relative paths. `requirements.txt` rides as `pyexcel/requirements.txt` so it sits next to the package without bloating the import surface. **`SystemPythonProbe`** locates the system Python the venv provisioner consumes: `PYEXCEL_PYTHON` env-override first (same variable `PyExcel.Excel.PythonResolver` honours, so one knob covers both halves), then PATH search. Windows-Store stub detection is explicit — `IsWindowsStoreStub(path)` checks the canonical `%LOCALAPPDATA%\Microsoft\WindowsApps\python.exe` location and flags any small (< 1 MiB) or reparse-pointed file there as the stub. The probe returns a typed `PythonProbeResult` (Found / FailureReason / VersionBanner) instead of throwing so the wizard renders the reason inline. **`VenvProvisioner`** runs `python -m venv .pyexcel-venv` in the project directory — matching the `PyExcel.Excel.PythonResolver` layout exactly (`Scripts\python.exe` on Windows, `bin/python` on POSIX) so a venv created here is immediately discoverable by the runtime kernel resolver. Idempotent: an existing venv whose python exists is left untouched; a half-built venv (directory present, python missing) is recreated rather than continued. No `--system-site-packages` so dependencies stay reproducible across machines (v1 inherited them). **`PipRunner`** invokes pip via `python -m pip` (never the bare shim, whose location depends on activation): `Install` with `--no-input --disable-pip-version-check` so pip never blocks on a prompt and never spams the version-check notice; `UpgradePip` as a best-effort step that's logged but never throws; `Show` for single-package presence checks. Every invocation runs through `ProcessRunner` so the streams hit the log. **`DependencyVerifier`** strictly checks each kernel-required module imports via `python -c "import X"` — no 80% threshold; every module in `RequiredModules = [pandas, numpy, pyarrow, plotly, matplotlib]` must succeed or the verifier reports failure with the missing list. Import success is the right test because pip-show metadata can satisfy presence while a corrupted wheel still raises `ImportError`. **`ProjectPathResolver`** classifies a project path: `\\?\UNC\` long-prefix normalised to the canonical `\\server\share\…` form before further checks; UNC flagged on Windows so the wizard can warn about shares that block executable creation; OneDrive/Consumer/Commercial roots detected via env vars (the OneDrive client sets all three on a typical Windows session) with directory-boundary safety so `C:\Foo` doesn't match a OneDrive root of `C:\Foobar`. Pure — no disk reads, no env writes. **`SetupService`** is the facade: `Run(projectPath)` returns a `SetupResult` carrying per-stage `SetupStep` entries in order (resolve-path → ensure-project-dir → probe-python → provision-venv → extract-kernel → pip-install → verify-dependencies). Failures short-circuit downstream stages but every step that did run is on the result; the facade never throws past the call site so the wizard can render a complete report. The pip self-upgrade is run inline as a best-effort warning, not as a tracked step, so an upgrade-fail doesn't show as red when the install succeeds. **Embed list lives in `PyExcel.Setup.csproj`** as one `<EmbeddedResource>` per kernel file plus the requirements file — `Include` uses the same `..\..\embedded\…` backslash style every other csproj uses for cross-platform path joining; MSBuild accepts both separators. **Wiring:** `PyExcel.sln` gains the new project at GUID `…A09` with matching Debug|x64/Release|x64 entries; `PyExcel.Addin.csproj` adds a `ProjectReference` so the Setup assembly + its embedded resources ship inside the .xll bundle; `PyExcel.Bridge.Tests.csproj` adds a `ProjectReference` so the new tests compile against `PyExcel.Setup` types. CI gets a new `Build PyExcel.Setup (netstandard2.0)` step on the Linux lane. **Tests:** 9 `ProcessRunnerTests` (8 quoter cases + 5 process-execution cases — cross-platform via host-specific commands: cmd.exe/echo on Windows, /bin/sh/echo on POSIX; cover exit-zero, non-zero, timeout-kill, cancellation-token-kill, ILog line capture), 5 `KernelResourceExtractorTests` (ships canonical 11-file set, writes to disk preserving the `pyexcel/` package directory, second-call skip, tamper-then-refresh, null/empty target throw), 6 `ProjectPathResolverTests` (null/whitespace, plain local, OneDrive-flag, outside-OneDrive, look-alike-prefix not flagged, Windows UNC + long-prefix normalisation), 4 `SystemPythonProbeTests` (Windows-Store stub heuristic is no-op on POSIX, empty path no-op, override-missing-file fails, override-real-python succeeds, PATH-search succeeds), 3 `VenvProvisionerTests` (null arg throws, creates venv matching resolver layout, second call returns AlreadyExists, layout helper returns OS-correct path), 3 `PipRunnerTests` (null path throws, missing-requirements throws, `Show pip` succeeds against fresh venv, `Show missing-package` fails), 4 `DependencyVerifierTests` (RequiredModules sanity, per-module status reported in order, all-importable on CI-installed host, failure-reason populated when import fails), 3 `SetupServiceTests` (end-to-end pipeline against real Python creates venv + extracts kernel + installs deps + verifies, empty-path fails at resolve, missing-python-override fails at probe). Total: 37 new C# tests added to the existing 632-strong suite. CI's Linux lane already installs all kernel deps for the kernel test suite, so dep-verification passes on green CI; tests bail silently when Python isn't on PATH so a local dev box without Python doesn't fail the suite. Working branch: `claude/vigilant-ptolemy-kfggh7`.

- **2026-06-10 — Phase 6 code-complete: the chart pipeline (Plotly → native chart, Matplotlib → embedded image).** Every Phase 6 checkbox closed in one slice; the COM render is the Windows smoke test, everything below it is built and green on both CI lanes. **Kernel side:** new `pyexcel.kernel.chart` module — `plotly_figure_to_spec(fig)` is the JSON port of v1's `PlotlyToExcelXMLConverter` traversal (schema documented in the module docstring, `version: 1`): chart meta (type from first trace, title, per-axis title/min/max/log, legend visible/position from `showlegend`+`legend.visible`+orientation, barmode), per-trace data as **native JSON arrays** (the v1 comma-joined CSV strings could be corrupted by values containing commas), per-trace style (series type, name, primary/secondary axis group, visibility incl. `"legendonly"` → hidden, line color/dash/width, marker size/color/shape with the v1 symbol mapping, fill color with the D3-category10 palette default for bar/column/histogram, opacity), `layout.shapes` vertical/horizontal lines → `event_line`/`threshold` annotations. Improvements over v1 the port made deliberately: pie traces read `labels`/`values` (v1 read `x`/`y`, which pie traces don't carry — v1 pie was silently empty), scatter-with-`fill` → `area`, scatter-with-marker-size-array → `bubble` with `data.size`, horizontal bar keeps the v1 x↔y swap. Histogram pre-bins via numpy (`xbins` start/end/size honoured when fully specified, else `auto`; midpoints + counts + `lo:hi` labels). Unsupported trace types (box / violin / heatmap / waterfall / candlestick / …) raise `UnsupportedChartTypeError(TypeError)` naming the type and the supported set — the worker's existing `BadReturnType` arm carries it to the user verbatim. `matplotlib_figure_to_image(fig)` renders SVG first, PNG (150 dpi) fallback, `TypeError` with the underlying render error if both fail. Detection is **duck-typed on `__mro__`** (`Figure` class from a `plotly.*`/`matplotlib*` module) so the kernel never imports either package; `arrow_io.encode` calls `convert_figure` at the door. New typed wire values `ChartSpec(json)` / `ChartImage(data, format)` in `types.py` (re-exported from `pyexcel.kernel`), riding as new Arrow shapes `chart` (1×1 string) and `image` (1×1 binary + `pyexcel-image-format` field metadata); both decode back symmetrically. **C# side:** `ArrowShape.Chart`/`.Image` + `ArrowMarshal` decode arms returning typed `ChartSpec`/`ChartImage` (wire-format violations throw `FormatException`, not silent nulls). Cross-platform contract layer: `ChartSpecParser` (parses via `CanonicalJson` — `PyExcel.Bridge` gains `InternalsVisibleTo("PyExcel.Excel")` — validating version, chart type, per-trace id uniqueness + x/y length agreement + series type against the supported set, legend position, annotation type/axis/value; every rejection names the offending element, optional attributes default), `ChartTraceData` (the `BuildXYForCategories`/`BuildXYNumericFlexible` port: categorical vs numeric-flexible shaping, ISO-date → OADate with an `XIsDate` flag for axis formatting, one non-date string flips the axis to categories, null/NaN pairs dropped, histogram prefers the kernel's `lo:hi` text labels as categories, no-usable-data → null so the builder skips the series), `ChartColor` (hex `#RRGGBB`/`#RGB`, `rgb()`/`rgba()` incl. float channels — Plotly themes emit these; v1 rendered them black — and the v1 named-colour set; unrecognised → black, never an exception). net48 `ChartBuilder`: `Build(sheet, spec, l, t, w, h)` ports the live `BuildChartFromXML` over late-bound `dynamic` (chart type map, per-series type incl. `barmode=stack` → stacked variants, secondary axis, `IsFiltered` hide with visual fallback, line/marker/fill styling, bubble sizes, per-point text labels, axes title/min/max/log with positive-bound guard + `yyyy-mm-dd` ticks when x was dates, legend, `barmode=overlay` → Overlap 100, annotation lines spanning the orthogonal axis scale); **orphan guard** wraps everything after `ChartObjects().Add` — on any failure the ChartObject is deleted before the exception propagates; cosmetic steps (colours, dashes, labels) are individually guarded and logged to `Trace` (Excel rejects some combinations per chart type — a missed colour must not cost the chart), structural steps throw. `EmbedImage` stages bytes through a temp file for `Shapes.AddPicture` (native size, saved with document, temp deleted after). **Routing:** `RangeRunner.WriteResult` — `ChartSpec` → parse (before touching the sheet) + `Build` anchored at the output range (multi-cell range = chart footprint, single cell = v1's 800×500 default); `ChartImage` → `EmbedImage` at the anchor. `PyRunFunction.ToExcelOutput` returns an explicit placeholder string for chart results (a UDF re-runs on every recalc — building a ChartObject per calc would litter the sheet; the message points at the Run Python button). `PasteService` declines chart payloads with an actionable message (`ToString()` artefacts are not data). **Tests:** 75 Python (`test_chart.py`: wire-type validation, duck-typed detection, full spec contract incl. every series-type mapping, histogram binning, annotations, unsupported-type errors, matplotlib SVG + forced-PNG-fallback + both-fail, Arrow round-trips for both shapes) — kernel suite 227 green; C# 38 `ChartSpecParserTests` + 25 `ChartTraceDataTests` + 24 `ChartColorTests` + 10 `ChartWireTests` (hand-built buffers — the C# encoder deliberately has no chart arm) + 3 e2e chart round-trips through a real kernel (`Plotly figure → ChartSpec → parsed doc`, `Matplotlib → svg ChartImage`, `Box trace → KernelException BadReturnType naming 'box'`) — full suite 632 green, net48 targets compile clean (verified on Linux via the reference-assemblies package). CI installs plotly+matplotlib on both lanes (they were already declared in `requirements.txt`). The Windows + Excel smoke test for the COM render (chart appears, image embeds, malformed spec fails clean with no orphan) is the only open item.

- **2026-06-09 — Phase 5 truly closed: destructive-paste overwrite confirmation.** Closed the last `[~]` checkbox on Phase 5. New cross-platform `PastePreflight` (in `PyExcel.Excel`) carries two pure helpers: `Footprint(decoded)` returns the `(rows, cols)` the paste will occupy (2-D array → its own dims, 1-D vector → single row, scalar → 1×1, null → (0,0) so the service short-circuits), and `RangeHasContent(value2Snapshot)` returns true if any cell in the COM hand-back is occupied. The cross-platform helper treats `null` and empty string as "no content"; everything else (numbers including zero, non-empty strings, booleans, errors) counts. Excel-DNA's `ExcelEmpty` / `ExcelMissing` sentinels are net48-only and stripped to null by the COM-bound caller before invocation — `RangeHasContent` itself doesn't reference those types so it remains netstandard2.0-clean. `PasteService.RunActivePaste` rewired: after decoding on the background task, the `QueueAsMacro` now (1) computes the footprint via `PastePreflight.Footprint`, (2) resizes the target-range anchor to that footprint, (3) reads the resized range's current `Value2`, (4) strips Excel sentinels via a local recursive walker (`ExcelEmpty` / `ExcelMissing` → null; `ExcelError` is preserved as content — an error cell is data the user might still care about), (5) calls `PastePreflight.RangeHasContent` on the cleaned snapshot, (6) if occupied, shows a `System.Windows.Forms.MessageBox` with `MessageBoxDefaultButton.Button2` so an accidental Enter clicks "No" — the prompt names the address, the footprint, and the source run-id so the user knows what's about to be clobbered, (7) on "No" logs the cancellation to `Trace` + `LogDisplay` and returns without touching the sheet; on "Yes" proceeds to write. The write path itself is unchanged shape-wise — `WriteToRange` now takes the resolved range (instead of resolving from the address again) so the read snapshot, the preflight, and the write all hit the same range without a second `Resize` call. Added `System.Windows.Forms` reference to `PyExcel.Excel.csproj` (net48-only — matches the existing `Microsoft.CSharp` convention; the assembly isn't in SDK-style net48's default reference set). 24 `PastePreflightTests` covering: `Footprint` for null / 2-D / 1-D / scalar / empty 2-D / empty 1-D, `RangeHasContent` for null / scalar-null / scalar-empty-string / scalar-number / scalar-zero / scalar-non-empty-string / scalar-bool, 2-D all-null / all-empty-strings / one-non-empty-cell / one-zero / mixed-null-and-strings, 1-D all-null / with-content / all-empty-strings, and the lower-bound-1 COM array case (uses `Array.CreateInstance` with explicit bounds to pin that the indexer-plus-`GetLowerBound` pattern handles Excel's 1-based hand-back correctly). The COM-bound prompt + sentinel strip themselves aren't unit-testable on Linux but the cross-platform decision logic they delegate to is fully covered. Phase 5's bug-disposition entry ("Destructive paste — add overwrite confirmation") is now ticked.
- **2026-06-09 — Phase 5 close: XLSX/XLSM/XLSB import via Excel COM + `path!Sheet` syntax.** Closed the last open Phase 5 checkbox. `ImportPlanner` now distinguishes CSV-shaped text from Excel binaries via a new `ImportFormat` enum (`Csv` / `Excel`); the planner's `Create()` routes through the appropriate decoder based on extension. **Path syntax:** the user can pin a sheet by appending `!SheetName` to the source path — `data.xlsx!Inputs`. With no `!`, the importer falls back to the first sheet. New `ParsePathAndSheet(input)` splits on the last `!` **only when the part before ends with an Excel-format extension** (`.xlsx` / `.xlsm` / `.xlsb`) — a CSV path with `!` in the filename is preserved verbatim, so the rule never accidentally hijacks a non-Excel path. An empty or whitespace-only sheet name is treated as "no sheet" (default first). New `DetectFormat(path)` returns the format or throws `FormatException` for explicitly-unsupported binaries: `.xls` (legacy binary — "save as .xlsx and try again") and `.ods` (OpenDocument — same message). `DetectDelimiter` is unchanged and still throws for all Excel-family extensions — it's reused by `ExportPlanner`, which still rejects Excel-format destinations. `ImportPlan` gains two positional fields (`Format`, `SheetName?`) — neither construction site outside the planner exists, so no callers broke. **`ImportService`** branches on `plan.Format`: the CSV path is the existing background-Task / `QueueAsMacro` flow; the Excel path runs all COM (open / read / close / write) inside a single `QueueAsMacro` since `Workbooks.Open` must run on the STA anyway, so there's no useful background slice. The Excel reader: pre-checks `File.Exists` on the main thread (a missing file fails fast without queuing an empty macro), saves and restores `app.ScreenUpdating` + `app.DisplayAlerts` around the read so a hidden Open doesn't flash the screen or fire a corrupt-file prompt, scans `app.Workbooks` for a matching `FullName` (case-insensitive) and **reuses an already-open workbook without closing it** — leaving the user's view alone — otherwise opens fresh with `UpdateLinks=0, ReadOnly=true` and closes it on the way out. Sheet selection: `wb.Sheets[1]` (one-based COM index) for the default first-sheet case, `wb.Sheets[sheetName]` for the named case (a `COMException` for a missing name is translated into a clean `FormatException` the service surfaces to LogDisplay). `UsedRange.Value2` is normalised through `NormalizeUsedRange` → a 0-based `object?[,]` rebased from Excel's 1-based array, with `ExcelEmpty` / `ExcelMissing` / `ExcelError` sentinels mapped to `null`. The write-back reuses the existing `WriteTable` helper (`anchor.Resize[rows, cols].Value2 = grid`). Tests: 30 `ImportPlannerTests` (up from 16) covering the new `DetectFormat` arm across CSV / TSV / TXT / no-extension / XLSX / XLSM / XLSB / XLSX-uppercase / `.xls` reject / `.ods` reject / null, plus 14 new `ParsePathAndSheet` cases (no-bang, each Excel extension with sheet, separator-only, whitespace-only sheet, trim sheet, CSV-with-bang preserved, no-extension-with-bang preserved, directory+sheet, internal-space sheet name, null input throws), plus 7 end-to-end `Create` composition cases (XLSX routes to Excel format with no sheet, XLSX-with-sheet-syntax routes to Excel format with sheet, XLSM-with-sheet, XLSB-with-sheet, `.xls` throws, `.ods` throws, CSV-with-`!`-in-name is preserved as-is). The COM-bound `RunExcelImport` itself isn't unit-testable on Linux but the planning + sheet-parsing + format-detection logic it delegates to is fully covered; the actual `Workbooks.Open` + `Sheets[name]` is the Windows smoke test for this slice. **WinForms sheet picker explicitly deferred** to Phase 8 — the `path!Sheet` syntax is the keyboard-driven interim, the picker is the UI polish for users who want to browse.
- **2026-05-30 — Phase 5 Paste: `PastePlanner` + `PasteService` + `ArchivedRun.HasOutput`.** The Paste button's COM-bound shell + the cross-platform selection rule it leans on. New `PastePlanner.Create(pasteOutput, workbookKey, recentRuns)` (cross-platform, pure logic) validates the user-typed target range and picks the archived run whose `output.arrow` the paste will decode: **same-workbook runs preferred**, then fall back to any output-bearing run (so a fresh workbook can still paste from a recent `=PY.RUN` in an unbound context). `FormatException` for blank target or "no recent run produced output". Selection consumes an already-materialised `IReadOnlyList<ArchivedRun>` snapshot rather than the live `RunArchive` instance so the planner stays pure (no I/O, no statics) and Linux CI drives it directly. New `PastePlan(SourceArrowPath, TargetRangeAddress, SourceRunId)` — `SourceRunId` is informational, surfaced to LogDisplay so the user can correlate the paste against the archive when debugging. New `ArchivedRun.HasOutput` (8th positional param, default `false` so the existing field-order is preserved) populated by `RunArchive.TryReadArchivedRun` via `File.Exists(output.arrow)`. **Distinct from `Status`** by design: a successful run that returned `None` is `Status=Success` but `HasOutput=false`; an errored run that emitted partial output before failing is `HasOutput=true` and still pasteable — file existence is the most reliable signal. New net48-only `PasteService.RunActivePaste(state)` (`#if NETFRAMEWORK`): same SAFE-1 shape as `ImportService` — planning on the main thread (`PyExcelServices.RunArchive.List()` + `PastePlanner.Create`), Arrow read + decode on a background `Task`, COM write-back via `QueueAsMacro`. Write semantics mirror `RangeRunner`: `object?[,]` resizes the anchor to its footprint and sets `Value2`; `object?[]` spills as a row (the archive doesn't preserve orientation metadata beyond what `ArrowMarshal.Decode` recovers); scalars drop into the top-left cell. Errors surface to `Trace` + `LogDisplay` with the run-id for context. **Overwrite confirmation explicitly deferred** to Phase 8 — the WinForms prompt is the right home for a destructive-action gate, not the COM service. Ribbon: `OnPaste` now routes through `PasteService.RunActivePaste(state)` instead of `StubAction`. Tests: 11 `PastePlannerTests` (blank-output throws, null-runs-list throws, leading/trailing whitespace trimmed, empty runs throws, no-output-bearing-run throws, newest-output-bearing-run wins, same-workbook preference beats newer unbound, no-same-workbook fallback to newest, null-workbook-key no-preference, plan-source-is-archive-dir-plus-output-arrow, plan-target-roundtrips); 2 new `RunArchiveTests` for the new field (`List_RunWithOutput_SetsHasOutputTrue`, `List_RunWithoutOutput_SetsHasOutputFalse`). The COM-bound `PasteService` itself isn't unit-testable on Linux but the planning + selection logic is fully covered; the actual Range write is the Windows smoke test for this slice.
- **2026-05-30 — Phase 5 Import / Export services + Shell launcher + ribbon wiring.** Five new source files in `PyExcel.Excel` + one in `PyExcel.Common.Shell` + ribbon callbacks lit up: the whole Import / Export half of Phase 5 reaches the "code-complete on net48, awaits Windows smoke test" milestone the rest of the COM-coupled work uses. **`ImportPlanner` / `ExportPlanner`** (cross-platform, pure logic) validate the user-typed ribbon fields and produce a typed plan record: `ImportPlan(AbsoluteSourcePath, Delimiter, TargetRangeAddress)` / `ExportPlan(SourceRangeAddress, AbsoluteTargetPath, Delimiter)`. Path resolution joins relative source/target paths against the active workbook directory (`Environment.CurrentDirectory` fallback when the workbook is unsaved). Delimiter detection from extension: `.tsv` → `\t`, anything else → `,`, binary spreadsheet extensions (`.xlsx` / `.xls` / `.xlsm` / `.xlsb` / `.ods`) throw `FormatException` with a clean "Excel-format import is a follow-up" message so a user typing `data.xlsx` doesn't get a mysterious CSV mis-parse. **`CsvCellTypeInference`** (cross-platform) is the import-side per-cell rule: `null`/empty → `null`, case-insensitive `TRUE`/`FALSE` → `bool`, invariant-culture `double.TryParse` → `double`, leading-zero and leading-plus strings stay as `string` to preserve SKU-like and phone-number-prefix-like formatting (parsing destroys leading zeros). **`CsvCellFormatter`** (cross-platform) is the symmetric export-side: `double` → invariant `"R"` round-trip so re-importing is loss-free, `bool` → `TRUE`/`FALSE` (matches the inference recogniser for round-trip), `DateTime` → ISO 8601 (`yyyy-MM-ddTHH:mm:ss`), `string` pass-through, fallback to `Convert.ToString(value, InvariantCulture)`. Locale-stability tests pin all three under `de-DE` to guarantee the export survives a German-locale Excel host. **`ImportService` / `ExportService`** (net48-only, `#if NETFRAMEWORK`) wrap the planners + parser/writer + COM. Same SAFE-1 contract as `RangeRunner`: planning + range I/O on the main thread, file I/O and parsing/writing on a background `Task`, write-back queued via `ExcelAsyncUtil.QueueAsMacro`. Errors surface to `Trace` + Excel-DNA's `LogDisplay` with actionable messages ("Import: file not found at …", "Export: directory not found for …", "Export: '…' resolved to an empty range"). `ExportService` `EnsureDirectory`s the target parent so a user typing a fresh path doesn't get a confusing `DirectoryNotFoundException`; `ExportService.FormatCell` strips Excel-DNA's `ExcelEmpty`/`ExcelMissing`/`ExcelError` sentinels (which `CsvCellFormatter` doesn't know about — those types are net48-only) then delegates the typed formatting. **`PyExcel.Common.Shell.ShellLauncher`** (net48-only) — three thin `Process.Start` wrappers: `Open(path)` uses `UseShellExecute=true` so the OS picks the registered handler (Markdown viewer for `.md`, default Python editor for `.py`, file manager for a directory); `OpenInExplorer(path)` either opens a directory plainly or uses Explorer's `/select,` verb to highlight a file (note the comma — required by Explorer's argument-parser quirk); arguments are wrapped in double quotes for UNC / spaces-in-path safety. **`IWorkbookContext` extended** with a new `CurrentWorkbookDirectory` property — implemented in `ExcelWorkbookContext` via the same late-bound COM pattern as the existing `CurrentWorkbookKey`, returning `null` for an unsaved workbook (empty `wb.Path`). `NullWorkbookContext.CurrentWorkbookDirectory` returns `null` so the default service slot doesn't surprise the ribbon. **Ribbon wiring:** `OnImport` / `OnExport` now route through their respective services (state.ImportInput/Output and state.ExportInput/Output are the user's typed fields); `OnReadMe` opens `<workbookDir>/README.md` when present, falling back to the previous Phase-1 in-Excel alert; `OnOpenExplorer` opens the workbook directory in Explorer; `OnEditPython` opens `<workbookDir>/userScripts/<selectedScript>` (the directory layout `ScriptDirectoryWatcher` is designed around). Each ribbon callback fails gracefully through `LogDisplay` when its preconditions aren't met (no active workbook, no script selected, file not found) — no exceptions escape into Excel's UI loop. Tests: 16 `ImportPlannerTests` (field validation, leading/trailing whitespace, absolute / relative / no-workbook-dir path resolution, delimiter detection across `.csv` / `.tsv` / `.txt` / no-extension / `.xlsx` family / `.ods` / `null`, end-to-end `Create` composition); 7 `ExportPlannerTests` (sibling coverage); 16 `CsvCellTypeInferenceTests` (empty / bool tokens / numbers / leading-zero / leading-plus guards / de-DE locale invariance / plain strings); 13 `CsvCellFormatterTests` (null / string pass-through / invariant doubles / bool round-trip with inference / ISO-8601 datetimes / de-DE locale invariance / fallback path). COM-bound services aren't unit-testable on Linux but the planning + cell-level logic they delegate to is fully covered; the actual Range I/O is the Windows smoke test for this slice.
- **2026-05-30 — Phase 5 state plumbing: Import / Export / Paste fields persisted through `StateService` + `WorkbookStateCodec` + wired ribbon callbacks.** Five new optional text fields on `WorkbookState` — `ImportInput`, `ImportOutput`, `ExportInput`, `ExportOutput`, `PasteOutput` (all `string?`, defaulting to `null`). Added as positional record parameters with default values so existing call sites that build a `WorkbookState` via `.Empty(key) with { … }` keep working unchanged. `StateService` gains five typed setters (`SetImportInput` / `SetImportOutput` / `SetExportInput` / `SetExportOutput` / `SetPasteOutput`) that route through the same `Update` path as the existing helpers — single coarse lock, `StateChanged` fired on write, mutator preserves all other fields. `WorkbookStateCodec` extended on both ends: serializer emits `<import-input>` / `<import-output>` / `<export-input>` / `<export-output>` / `<paste-output>` under the existing `urn:pyexcel:state:1` namespace (each omitted when null, matching the existing pattern for `<py-input>` etc.); deserializer reads them back, defaulting to `null` if the element is absent. **Schema version stays at 1** — the new elements are additive, so a document written before this change still parses cleanly (the missing elements decode to `null`), and a document written after still satisfies the v1 namespace contract for any reader that doesn't know about the new fields. A pre-Phase-5 backward-compat test pins this. Wired five formerly-stub ribbon callbacks: `OnImportInputChange` / `OnImportOutputChange` / `OnExportInputChange` / `OnExportOutputChange` / `OnPasteOutputChange` now persist through `StateService` instead of `StubChange`-logging; the matching `GetImport*` / `GetExport*` / `GetPasteOutput` getters read from `ActiveState()` instead of returning `string.Empty`. The COM-bound `ImportService` / `ExportService` / `PasteService` (Phase 5 follow-ups) will read these fields on click — they're the input the user types in the ribbon, the COM handler is just the consumer. Unused `StubChange` helper deleted now that nothing references it (production-grade bar: no dead code). Tests: 8 new `StateServiceTests` (each new setter preserves other fields, fires `StateChanged`, accepts null, null-key throws) + 5 new `WorkbookStateCodecTests` (full round-trip with all five new fields, empty-state round-trip leaves them null, null fields are not emitted as elements, pre-Phase-5 document decodes new fields as null without throwing, empty-string round-trips as empty string distinctly from null). Ribbon-side wiring isn't unit-testable (Excel-DNA `IRibbonControl` needs the host) but the state plumbing it delegates to is covered.
- **2026-05-30 — Phase 5 foundation: RFC-4180 CSV/TSV parser + writer.** Started Phase 5 with the cross-platform piece both the planned `ImportService` and `ExportService` sit on top of. New `PyExcel.Excel.CsvParser` is a single-pass state-machine decoder returning `IReadOnlyList<IReadOnlyList<string>>`: RFC 4180 plus the four Excel/Python extensions real CSV needs — all three line endings tolerated within a single file (`CRLF` / `LF` / bare `CR`, with `CRLF` consumed as one terminator), a leading UTF-8 BOM consumed silently (string overload checks `text[0] == '﻿'`; `Stream` overload delegates to `StreamReader` with `detectEncodingFromByteOrderMarks: true` so the bytes don't even reach the decoded string), quoted fields with embedded delimiters / newlines / `""`-escaped quotes, and the permissive "content after closing quote is appended" rule that matches Python's `csv.reader` default mode and Excel's behaviour (`"a"b,c` → `["ab", "c"]`, not a parse error). Blank-line semantics match Python: a blank line between records emits `[]`, a trailing newline at EOF does *not* — we track `rowHasContent` so the final-record commit fires only when the row has any committed field or in-progress buffer. Spaces inside fields are preserved verbatim per RFC §2.4 (no trimming). Validation errors throw `FormatException` (`"unterminated quoted field"`) or `ArgumentException` for a delimiter that's a quote or newline (structurally invalid). New `PyExcel.Excel.CsvWriter` is the symmetric encoder: minimal RFC-4180 quoting (a field is wrapped iff it contains the delimiter, `"`, `\r`, or `\n`; internal `"` doubled), configurable line terminator (defaults to `CRLF` per the spec; `"\n"` for Unix), optional UTF-8 BOM prepend (off by default — the caller turns it on for "double-click in Excel-on-Windows" output where the system codepage would otherwise win), and a `Stream` overload that writes UTF-8 without the encoder's BOM (so `writeBom` is the sole control). Both halves share a common `ValidateDelimiter` guard. Pure logic — no Excel COM, no `IDisposable` ownership beyond `using var` on the stream wrappers (`leaveOpen: true` on both reader and writer so the caller's stream isn't closed). Tests: 38 `CsvParserTests` methods / 42 cases (all line endings, mixed terminators, trailing-newline / blank-line / EOF rules, BOM stripping including mid-field preservation, quoted-with-comma / quoted-with-quote / quoted-with-LF / quoted-with-CRLF / empty-quoted, leading / trailing / consecutive empty fields, whitespace preservation, permissive bare-quote and content-after-closing-quote, unterminated-quote → `FormatException`, invalid-delimiter → `ArgumentException`, null → `ArgumentNullException`, tab + semicolon alt delimiters, stream-overload with UTF-8 BOM bytes, leave-open semantics, realistic multi-row Excel-export sample); 28 `CsvWriterTests` methods / 30 cases (null guards, plain pass-through, every quote-trigger character, space-no-trigger rule, null-field-as-empty, default `CRLF`, `LF` override, no-trailing-newline rule, BOM emission as a literal `﻿` char and as `EF BB BF` bytes through the stream overload, tab + semicolon delimiters, stream leave-open); plus 2 parser↔writer round-trip property tests pinning that the encoder produces what the decoder accepts (commas, quotes, embedded newlines, leading/trailing whitespace, TSV with embedded tabs). The remaining Phase 5 surface (Excel-COM Import/Export/Paste services, ribbon wiring, shell helpers) is net48-only and awaits Windows CI to build.
- **2026-05-30 — Phase 4: archive a run — `RunArchive` + retention cap.** Closed the last open cross-platform Phase 4 checkbox. New `PyExcel.State.RunArchive` is the on-disk diagnostic store: each call to `Archive(RunArchiveEntry)` writes a directory `{root}/{yyyyMMddTHHmmssfff}_{8-hex-rand}/` containing the encoded inputs (`input_0.arrow`, `input_1.arrow`, …, Arrow IPC bytes, one per positional argument), the output (`output.arrow`, present iff the run returned a non-`None` payload), the formatted error block (`error.txt`, the same `KernelErrorRecord.FormatForClipboard()` the ribbon's Copy Last Error button uses — present on `Error`/`Cancelled`), and a line-per-field manifest (`manifest.txt`: `RunId`, `TimestampUtc`, `DurationMs`, `Source`, `WorkbookKey` (omitted when unbound), `ScriptPath`, `Function`, `InputCount`, `Status`, plus `ErrorCode`/`ErrorType`/`ErrorMessage` on failures — multi-line error messages collapsed to a single line so the file stays parseable; the full traceback lives in `error.txt` next door). Retention cap defaults to 20 (FIFO eviction by directory-name lex order, which matches chronological because the run-id leads with the UTC timestamp); pruning runs synchronously at the tail of every `Archive` call under the service's single coarse lock so concurrent writers can't trip each other's eviction. New `RunArchiveEntry` (record carrying timestamp + workbook key + script + function + source + duration + inputs + output + error + status), new `ArchivedRun` (record returned by `List()` — parsed headline fields plus the directory path), new `RunArchiveContext` (the per-callsite wrapper threading the archive instance + a `Source` label + the current workbook key through), new `RunArchiveStatus` enum (`Success` / `Error` / `Cancelled` — split out because the kernel reports cancel distinctly and a replayer wants to know "did the user pull the rug" vs. "did the script blow up"). Wire-in: `PyRun.Execute` / `ExecuteMany` / `ExecuteAsync` / `ExecuteManyAsync` gain an optional `RunArchiveContext? archive = null` trailing parameter; when supplied, a `Stopwatch`-timed try/finally captures the kernel exchange and `ArchiveBestEffort` writes a `RunArchiveEntry` (success, kernel error with `KernelException` mapped to `Cancelled`-vs-`Error` by code, host error, or `OperationCanceledException` → `Cancelled`). The archive write is wrapped in its own try/catch so an I/O fault in the side path can't mask the user-facing result or the in-flight exception. `PyRunFunction.RunCoreAsync` and `RangeRunner.RunActiveScript` build their archive contexts from `PyExcelServices.RunArchive` + the active workbook key, tagging Source `"PY.RUN"` and `"Run Python button"` respectively. New `PyExcelServices.RunArchive` static slot defaults to a `RunArchive` rooted at `%LOCALAPPDATA%\PyExcel\runs` (XDG-derived on Linux, `Path.GetTempPath()` fallback when neither is set) — lazy directory creation so the default service doesn't litter the FS on a fast-path exit. Coverage: 22 `RunArchiveTests` (construction validation, success/error layout, manifest field-by-field, multi-line error message collapse, retention prune, `MaxRuns=0` edge, `List()` ordering + manifest parse, corrupt-manifest skip, 48-write concurrent prune cap); 3 e2e `PyRunTests` through a real kernel (`Execute_WithArchiveContext_ArchivesSuccessfulRun`, `…_ArchivesKernelError`, `…_NoArchiveContext_DoesNotArchive`).
- **2026-05-30 — Phase 4: better error UI — `ErrorService` + ribbon Show/Copy Last Error.** Promoted the "Surface kernel errors" bullet from the partial state (LogDisplay only) toward done by adding the ribbon-side surface the partial entry called out as missing. New `PyExcel.State.ErrorService` keeps the last error per workbook key plus a global fallback slot for failures that arrive without a bound workbook (kernel boot, add-in init). New `KernelErrorRecord` is the immutable carrier — timestamp, source label, kernel `Code` / `PythonType`, `Message`, full `PythonTraceback`, optional `ScriptPath` — with `FormatForClipboard()` rendering a stable multi-line block ready to paste into a bug report. `PyRunFunction.RunCoreAsync` and `RangeRunner.RunActiveScript` route their existing `KernelException` / `Exception` catches through the service (best-effort, swallowed if the registry throws — the cell-level `#VALUE!` is never blocked by error-recording). Ribbon gains an Errors group (`grpErrors`) with **Show Last Error** (writes the formatted block to Excel-DNA `LogDisplay` and `Show()`s the window) and **Copy Last Error** (`System.Windows.Forms.Clipboard.SetText` of the formatted block — ribbon callbacks already run on Excel's STA main thread). `getEnabled` is `RibbonHasError`, independent of the `RibbonEnabled` workbook-level toggle, so errors surface even when PyExcel is disabled. `PyExcel.Ribbon.csproj` gains an explicit `<Reference Include="System.Windows.Forms" />` (SDK-style net48 doesn't auto-reference it). Coverage: 11 `ErrorServiceTests` (per-workbook scoping, global fallback, override semantics, replace-on-record, clear-fires-event-even-if-empty, format-includes-every-field, format-omits-redundant-type, format-omits-empty-optionals, null-record-throws). Still outstanding on this bullet: per-script error pane + cell hover tooltip (both Phase 8 WinForms).
- **2026-05-30 — Phase 4: `ExcelFormula` A1-mode round-trip (marshalling layer).** Closed the Phase 4 "`ExcelFormula` round-trip" bullet. New typed wrapper on both sides — Python `pyexcel.kernel.Formula(text)` (frozen dataclass, validates leading `=`) and C# `PyExcel.Excel.Formula(text)` (sealed class with ordinal equality) — backed by an Arrow string column whose field carries `pyexcel-cell-type=formula` metadata. Encoders detect the type: pyarrow's `_value_to_table` routes a scalar `Formula` and an all-`Formula` list/tuple through string Arrow with the marked field (mixed `Formula`/non-`Formula` lists are rejected — the marker is per-column, so there's no clean wire representation). C# `ArrowMarshal.ScanTypes` gains a `canFormula` arm + `BuildColumn` emits the formula metadata when every non-null cell is a `Formula`. Decoders thread the schema through `DecodeTable` / `DecodeVector` / `DecodeScalar`: any field with the marker wraps cells in `Formula(text)` rather than returning the raw string. Python's `_table_to_python` handles mixed-column tables (some formula columns, some regular) by stitching per-column Series so non-formula columns keep their Arrow-inferred dtype. **Out of scope:** DataFrame-of-Formula encoding (skipped — `pa.Table.from_pandas` flattens object Series to plain strings; the host would need to construct the Arrow table by hand) and the actual `Range.Formula = …` write hook in `RangeRunner` (the marshalling round-trip is what this item delivered; the COM-bound write is a separate ribbon-integration follow-up). Tests: 7 C# unit tests cover scalar, vector with nulls, mixed-with-double-column table, the mixed-within-column string fallback, plus Formula equality / validation; 8 Python tests cover scalar / vector / mixed-list-rejection / DataFrame-skipped / dataclass invariants. 2 end-to-end PyRun tests pin the cross-language round-trip through a real kernel — `from pyexcel.kernel import Formula; def transform(): return Formula('=SUM(A1:B2)')` decodes as a C# `Formula("=SUM(A1:B2)")`. Python lane verified locally (152/152 + 1 intentional skip); C# side awaits CI.
- **2026-05-30 — Phase 4: Range↔Arrow date/null/formula round-trip tests + date-decode bug fix.** Closed the Phase 4 "tests for dates, nulls, formulas" bullet, and surfaced a real bug while doing it. `ArrowMarshal.ReadCell` was falling through to its last-ditch `_ => array.GetType().Name` arm for `Date32Array` / `Date64Array` / `TimestampArray`, so a Python script returning a `datetime.datetime` would render the literal string `"TimestampArray"` in the user's cell. The new decoder handles all four `TimeUnit`s for `TimestampArray` plus `Date32` / `Date64`, landing on a naive `DateTime` (`DateTimeKind.Unspecified`) — pyarrow's default for a Python `datetime` is `timestamp[us]` with no timezone, and that's every path PyExcel currently exercises (matching pandas's `datetime64[ns]` → `timestamp[ns]` via the nanosecond arm). New tests: 7 unit tests in `ArrowMarshalTests` build `Date32` / `Date64` / `TimestampArray` buffers directly via Apache.Arrow's builders (the C# encoder doesn't produce these types, so coverage is decode-only) and assert the right `DateTime` falls out; 4 end-to-end tests in `PyRunTests` pin the cross-language round-trip via a real kernel — `datetime.datetime` → `timestamp[us]`, `datetime.date` → `date32`, list-of-datetime → vector spill, `pd.to_datetime` series → `timestamp[ns]`. Null coverage gained 3 unit tests (nulls embedded in numeric / bool / vector columns survive C#-only round-trip) and 2 e2e tests covering each leg in isolation (`pandas.df.isna().sum()` proves C# nulls reach the kernel as missing; `[1.0, None, 3.0]` from Python proves kernel nulls decode to C# null) — the full-table round-trip is fragile because `to_pandas` converts Arrow nulls to NaN, so we test each direction instead. Formula-result coverage adds 3 regression tests pinning that `=SUM`-style doubles and `=TEXT`-style strings round-trip via the per-column type inference and that mixed-type columns (a `#DIV/0!` string sitting in a numeric column) fall back to string without throwing. Single-locale assumption noted: the string-fallback path uses `double.ToString()` which is locale-dependent; the formula tests use whole numbers (`1`, `3`) that stringify identically across locales.
- **2026-05-30 — Phase 4 #4: UDF → kernel cancel bridge.** Closed the open polish item from `docs/phase3-and-4-completion.md` §4. `PyRunFunction.Run` now routes through `ExcelAsyncUtil.Observe` instead of `ExcelAsyncUtil.Run`: a small `IExcelObservable` (`PyRunObservable`) starts the work on the threadpool with a `CancellationTokenSource`, and returns a `CancelOnDispose(cts)` to Excel-DNA. When Excel-DNA disposes the subscription (formula change, workbook close, cell delete), the CTS is cancelled; the token registration inside `KernelClient.RunAsync` pushes a `CANCEL` frame to the kernel, which sets `is_cancelled()` and replies `ERROR/Cancelled`. The handler swallows that as a no-op (the observer is already gone). `KernelException` and other faults still surface as `#VALUE!` + LogDisplay+Trace, matching the prior contract. New async overloads `PyRun.ExecuteAsync` / `ExecuteManyAsync` keep the cancellation token plumbed through the marshal-and-dispatch core; the sync `Execute` / `ExecuteMany` are unchanged so `RangeRunner` (the ribbon button) stays put. Tests: `RunAsync_Cancellation_Surfaces_OperationCanceledException` covers the supervisor-pumps-CANCEL path at the `KernelClient` level; `ExecuteAsync_TokenCancelledMidRun_ThrowsOperationCanceled` covers the same end-to-end through `PyRun`; happy-path tests pin that the async wrappers preserve sync semantics. The Excel-DNA observable itself (`PyRunObservable`) is `#if NETFRAMEWORK` and isn't unit-testable without Excel-DNA — its CI coverage is "the net48 build still compiles with the new wiring". Phase 4's polish list now has only #5's WinForms progress dialog left (Phase 8).
- **2026-05-29 — Phase 3 #2 `AppEventSink` + #3 COM-side CustomXMLPart persistence.** Closed the two COM-coupled Phase 3 tails. New `AppEventSink` (`PyExcel.Addin`, net48) subscribes to `Application.{WorkbookOpen, WorkbookActivate, WorkbookBeforeSave, WorkbookBeforeClose, SheetActivate}`: Open restores state from the workbook's `CustomXMLPart` + repaints the ribbon; Activate repaints (the active key changed but nothing in the registry mutated, so no `StateChanged` fires on its own); BeforeSave flushes state into the part; BeforeClose flushes + `StateService.Forget`; SheetActivate → `SetCurrentSheet`. Every handler runs inside a `Guard` so a fault can't propagate into Excel's event pump. New `WorkbookStatePersister` (net48) is the thin COM shell over `CustomXMLParts.SelectByNamespace`/`Add`/`Delete`, delegating the XML round-trip to the already-tested `WorkbookStateCodec` (new pure helpers `SerializeToString` + `TryDeserialize`, the latter returning `false` on null/foreign/corrupt/wrong-version XML so a bad part can never throw into a COM handler). **Design fork resolved (PIA vs. late-bound):** app events can't be wired over a late-bound `dynamic` (`+=` needs a typed delegate), so this is the one place PyExcel takes a typed Office interop reference — via the `ExcelDna.Interop` NuGet package — restorable on the Excel-less Windows CI runner (unlike an *installed* PIA), with its own MSBuild `.targets` wiring up the references. That dissolves the doc's only objection to the typed route; everything else COM-bound (`ExcelWorkbookContext`, `RangeRunner`) stays late-bound. *(The package's implicit embed-via-`.targets` mechanism didn't surface the Office types on the Windows CI runner — CS0234 — so the PIA assemblies it restores (`lib/net452`) are referenced explicitly with `EmbedInteropTypes` instead; see `PyExcel.Addin.csproj`.)* `WorkbookKeys` now owns the shared session GUID + saved-vs-unsaved key rule so the sink and `ExcelWorkbookContext` derive **identical** keys (required for correctness — otherwise per-workbook state / `Forget` / persist would target different entries); `ExcelWorkbookContext` refactored onto it. Ribbon gained `PyExcelServices.RequestRibbonInvalidate` (registered in `RibbonOnLoad`) so the sink can repaint on Activate without reaching into the ribbon class. **Verification:** the cross-platform logic is unit-tested — `WorkbookKeysTests` (8), `WorkbookStatePersistenceTests` (9), `PyExcelServicesTests` (1) — and builds on both CI lanes; the COM plumbing (`AppEventSink`, persister) is compiled by the Windows CI lane (full-solution build, NuGet-restored PIA) but its **runtime behaviour still needs the Windows + Excel smoke test** in `docs/phase3-and-4-completion.md` §2/§3 — *not run yet* (this was a Linux session with no dotnet/Excel). Closes Phase 3's "state survives save/reopen" exit criterion pending that smoke test.
- **2026-05-29 — Phase 4/#5 kernel half: `report_progress` → `PROGRESS` frames.** Closed the Phase 2 "worker doesn't emit PROGRESS" gap. New user-facing `pyexcel.kernel.report_progress(percent=None, message="")` (re-exported from `pyexcel.kernel` next to `is_cancelled`): a long-running `transform()` calls it to push status to the host. Mirrors the cancellation plumbing — `worker._begin_job(event, progress_sink)` installs a per-job sink, `report_progress` forwards `(float|None, str)` to it (inert no-op when no job is in flight), `_end_job` clears it. The supervisor's `_run_with_cancellation` owns a `queue.Queue` the sink enqueues onto from the worker thread; the main loop drains it to `PROGRESS` frames via a new `_flush_progress` helper — every wire write stays on the main thread (single writer, no race with PONG/terminal), flushed each ~50 ms tick and once more after the worker joins so all `PROGRESS` precede the terminal `RUN_RESULT`/`ERROR`. Meta matches `KernelClient.RaiseProgress`: `run_id`, `percent` (JSON `null` = indeterminate), `message`. Pure Python — no C# touched; `FrameType.Progress` and `KernelClient.ProgressReceived` were already wired. 5 worker unit tests + 1 e2e supervisor test; **146/146 kernel tests green**. Remaining for #5 is the WinForms progress dialog (Windows-only, Phase 8); the C#-only open items (#2 `AppEventSink`, #3b CustomXMLPart persister, #4 UDF cancel bridge) are untouched — they need a Windows + .NET SDK box this Linux session doesn't have.
- **2026-05-29 — Phase 4 VERIFIED on real Excel + the packaging fixes that took to get there.** Smoke-tested the whole COM path end-to-end on Windows + 64-bit Excel: Enable PyExcel → type Script/Input/Output → Run → result spills back. All three cases pass — single-table doubling (`A1:B2`→`D1:E2`), error surfacing (`raise ValueError` shows the full Python traceback in Excel-DNA LogDisplay, sheet untouched), and multi-input (`prices=A1:A2; signals=B1:B2` → two positional args). This closes the Phase 4 exit criterion. Five fixes were needed that CI could not have caught (no Excel on the runners):
  - **`Microsoft.CSharp` reference** on the net48 targets of `PyExcel.Addin` + `PyExcel.Excel` — `dynamic` COM access needs the runtime binder, which SDK-style net48 projects don't reference by default (CS0656). *This one would have failed the Windows CI build too.*
  - **`.dna` `<ExternalLibrary>` entries for `PyExcel.Ribbon.dll` + `PyExcel.Excel.dll`** — Excel-DNA only scans listed libraries for `[ExcelRibbon]` / `[ExcelFunction]`; a merely-referenced dependency is packed but not scanned, so the ribbon tab never rendered (AutoOpen ran but `RibbonOnLoad` never fired) and `=PY.RUN` wasn't registered.
  - **`OnEnablePyExcel` wired to toggle `Enabled`** — was a Phase-1 stub, so every `getEnabled=RibbonEnabled` control stayed greyed out with no way to turn the add-in on.
  - **`app.config` binding redirects** for Apache.Arrow's `System.*` deps (Unsafe 4.0.6.0, Memory 4.0.1.2, Buffers 4.0.3.0, Numerics.Vectors 4.1.4.0, Tasks.Extensions 4.2.0.1) — net48 doesn't auto-unify like .NET Core, so the first `ArrowMarshal` call threw `0x80131040`. ExcelDna copies app.config → `PyExcel-AddIn64.xll.config`.
  - Confirmed `ExcelWorkbookContext` (#1) returns sane per-workbook keys in the live host. #1 and #7 are now verified, not just code-complete.
- **2026-05-25 — Phase 4 step 8: `OnRunPython` ribbon button (Phase 4 exit criterion).** New `PyExcel.Excel.RangeRunner` (net48-only, `#if NETFRAMEWORK`) drives the button end-to-end: on the main thread it reads the configured input ranges off the active sheet (re-basing Excel's 1-based `Value2` arrays to 0-based `object?[,]`, scalars pass through), then dispatches `PyRun.ExecuteMany` on a background `Task` (SAFE-1: the callback returns before any pipe traffic), then writes the decoded result back into the `PyOutput` range via `ExcelAsyncUtil.QueueAsMacro` (a table resizes the anchor to its footprint, a scalar drops into the top-left, a `None` return writes nothing). All COM is late-bound `dynamic` on `ExcelDnaUtil.Application` — no Office PIA, so the PIA-less Windows CI build still compiles. `PyExcelRibbon.OnRunPython` now delegates to it (PyExcel.Ribbon gains a project ref to PyExcel.Excel; PyExcel.Excel gains one to PyExcel.State for `RibbonRangeParser` + `WorkbookState`). Errors surface to `Trace` + Excel-DNA `LogDisplay`, same contract as the `=PY.RUN` UDF. Can't be CI-tested (COM range I/O); the step-by-step Windows smoke test is in `docs/phase3-and-4-completion.md`. The remaining open items are #2 `AppEventSink` (blocked on a PIA-vs-late-bound design decision — the PIA breaks CI), #4 cancel bridge, #5 progress UI.
- **2026-05-25 — Phase 3 step: `ExcelWorkbookContext`.** Concrete `IWorkbookContext` backed by `Application.ActiveWorkbook` over the Excel COM interop. Uses `ExcelDnaUtil.Application` dispatched through `dynamic` so `PyExcel.Addin` doesn't need an Office PIA reference. Key strategy: saved workbooks → `Workbook.FullName`; unsaved (empty `Path`) → `"unsaved:{SessionGuid}:{Workbook.Name}"` where `SessionGuid` is allocated once per add-in load so `Book1` / `Book2` don't collide. Any COM exception during lookup yields `null` (the ribbon's getters all tolerate that as "no workbook"). Wired into `PyExcelServices.WorkbookContext` from `AddIn.AutoOpen`. Can't be CI-tested (no Excel COM on Linux); manual smoke test on a Windows + Excel dev box is the verification path tracked in `docs/phase3-and-4-completion.md` §1.
- **2026-05-25 — Phase 3 step: `WorkbookStateCodec`.** Pure-XML round-trip for the user-editable slice of `WorkbookState`. Schema is hand-rolled `XDocument`: root `<pyexcel state-version="1" xmlns="urn:pyexcel:state:1">` carrying `<enabled>`, optional `<selected-script>` / `<py-input>` / `<py-output>` / `<selected-action>`, and an `<actions>` list whose elements have `name`/`script`/`input`/`output` attributes plus an optional `<kwargs>` child sorted by key for byte-stable output. Persisted fields are the ones the user types into the ribbon; transient ones (`CurrentSheet`, `AvailableScripts`, `WorkbookKey`) stay out of the XML — the caller supplies the key on Deserialize so a workbook-saved-as-copy gets the right new key, and the live sources (sheet-activate event, `ScriptDirectoryWatcher`) repopulate the transients. Errors throw `FormatException`: wrong root, wrong namespace, missing/unsupported `state-version`, missing required `<action>` attribute. 15 round-trip + error-path tests in `WorkbookStateCodecTests.cs`. The COM-side persister (`CustomXMLPart` read/write on `WorkbookOpen` / `WorkbookBeforeSave`) is the Windows-only follow-up tracked in `docs/phase3-and-4-completion.md` §3.
- **2026-05-25 — Phase 4 step 7: `RibbonRangeParser`.** Pure-logic parser for the ribbon's Input / Output text fields. Accepts the `prices=A1:C10; signals=D1:D10` multi-binding syntax plus the existing anonymous `A1:C10` form; returns an ordered list of `RangeBinding(Name?, RangeText)` records. Whitespace tolerance around separators and `=`; empty entries (trailing/leading/double `;`) silently skipped. Malformed input throws `FormatException`: empty name before `=`, empty range after `=`, or duplicate name (case-sensitive — matches the rest of the codebase). 15 unit tests in `RibbonRangeParserTests.cs`. Unblocks #7 (`OnRunPython` ribbon button) per `docs/phase3-and-4-completion.md`.
- **2026-05-24 — Phase 3 foundation: state + ribbon wiring + script watcher.** New `PyExcel.State` assembly (multi-targets net48 + netstandard2.0, no Excel-DNA dependency in the core so it tests cross-platform).
  - `StateService` — process-wide registry of immutable `WorkbookState` records keyed by workbook id, single coarse lock for mutation, `StateChanged` event for ribbon-invalidation hooks. Typed helpers for every ribbon edit (SetEnabled / SetSelectedScript / AddAction (upsert) / DeleteAction / …).
  - `WorkbookState` + `RibbonAction` records — immutable, with-clone-based mutation, `SelectedAction` convenience accessor.
  - `IWorkbookContext` — abstraction over "which workbook is active", `NullWorkbookContext` default keeps the ribbon sane before the add-in's `AutoOpen` wires the real implementation.
  - `ScriptDirectoryWatcher` — wraps `FileSystemWatcher` against a `userScripts/` dir, normalises the `.py` list (deduped, sorted, name-only), pushes an initial snapshot synchronously so consumers don't sit at empty until the first edit lands.
  - `PyExcelServices` static service locator so Excel-DNA's parameterless-ctor `PyExcelRibbon` can pull dependencies without DI gymnastics.
  - `PyExcelRibbon`: every formerly-hardcoded getter (`RibbonEnabled`, `GetScriptCount`, `GetScriptLabel`, `GetScriptText`, `GetPyInput`, `GetPyOutput`, `GetActionCount`, `GetActionLabel`, `GetActionText`) reads from `PyExcelServices.State.Get(activeKey)`. `OnPyInputChange` / `OnPyOutputChange` / `OnScriptChange` / `OnActionChange` write back. `OnDeleteAction` now actually deletes. `OnAddAction` / `OnEditAction` still stubbed pending the EditActionForm (Phase 8); their state plumbing is in place. `RibbonOnLoad` subscribes to `StateChanged` and queues `IRibbonUI.Invalidate` via `ExcelAsyncUtil.QueueAsMacro` so changes originating from a worker thread (`FileSystemWatcher`, Excel COM events, ...) refresh the ribbon safely.
  - Tests: 17 `StateServiceTests` covering empty defaults, mutator-preserves-fields, multi-workbook isolation, Forget semantics, Action upsert / delete-and-clear-selection, argument validation. 7 `ScriptDirectoryWatcherTests` against a tmp directory exercising initial snapshot, add/delete/rename events, manual `Refresh`, non-`.py` filter, argument validation, idempotent `Dispose`.
  - The CustomXMLPart persistence + `AppEventSink` (COM event sink) + the routing of Excel-DNA UDF cancellation through `KernelClient.Cancel` are the COM-coupled tail; tracked in `docs/phase3-and-4-completion.md` for the next session.
- **2026-05-24 — Phase 4 step 5: cooperative CANCEL.** The supervisor now dispatches `RUN_REQUEST` on a worker thread and pumps inbound frames in the main loop, so `CANCEL` (and `PING`) arriving during a run actually land. The cancel flag is exposed to user code as `pyexcel.kernel.is_cancelled()`; long-running scripts can poll it between work units and return early. Either way (cooperative abort or natural completion), the kernel surfaces `ERROR / Cancelled` so the host can tell the run was interrupted. Two new module-level functions in `worker.py` (`_begin_job` / `_end_job` / `is_cancelled`), one new method on `FrameTransport` (`has_data(timeout_s)`) backed by `select` on POSIX and a `PeekNamedPipe` ctypes call on Windows, plus the `_run_with_cancellation` helper in `supervisor.py`. Three end-to-end pytest tests cover: CANCEL during a long run → `ERROR / Cancelled`, unsolicited CANCEL → polite `ERROR` + loop stays alive, PING during a run → `PONG` arrives. 140/140 kernel tests green. The Excel-DNA UDF doesn't route Excel-DNA's task cancellation to `KernelClient.Cancel` yet — that bridge is a follow-up.
- **2026-05-24 — Phase 4 step 4: error visibility via LogDisplay.** `PyRunFunction` now writes kernel-error details (`Code`, `PythonType`, `Message`, full `PythonTraceback`) to `ExcelDna.Logging.LogDisplay` in addition to `Trace`. LogDisplay is Excel-DNA's built-in pop-up log window — surfaces in Excel itself rather than only in DebugView. Cell still gets `#VALUE!` so `ISERROR()` keeps working. No code change required of users; just a packaging upgrade to a more useful error path.
- **2026-05-24 — Phase 4 step 3: SAFE-1 async UDF.** Replaced the sync UDF body with `ExcelAsyncUtil.Run(...)` so the calc thread no longer blocks for the duration of a kernel run. First call returns `#N/A` immediately; Excel-DNA refreshes the cell when the background task completes. Identical inputs short-circuit to the cached result instead of re-spawning. The blocking work now runs in `RunSynchronously` on Excel-DNA's worker thread; same error contract as before (KernelException → `#VALUE!` + `Trace.WriteLine` of the Python traceback). Worker-side cancellation (kernel acting on CANCEL frames) is still pending — Excel-DNA cancels its background task on formula change, but the kernel completes the run regardless.
- **2026-05-24 — Phase 4 step 2: `=PY.RUN` UDF.** Added `PyRunFunction.cs` (net48-only, `#if NETFRAMEWORK` so the netstandard2.0 / Linux CI slice ignores it). ExcelDna.Integration 1.8.0 conditional package reference for the net48 target. The UDF is a small translation layer over `PyRun.Execute` — Excel sentinel arguments map to `null`, the `PyRun.EmptyResult` sentinel maps to `ExcelEmpty.Value`, `KernelException` is rendered as `#VALUE!` in the cell with the Python traceback logged via `Trace.WriteLine`. Synchronous in this slice; async/progress/cancel (SAFE-1) and the ribbon button are separate Phase 4 items. No automated test coverage on this file alone — exercising `[ExcelFunction]` needs an actual Excel instance — but the dispatch core it delegates to is covered by `PyRunTests`' 13 end-to-end cases.
- **2026-05-24 — Phase 4 step 1: marshalling + dispatch core.** New `PyExcel.Excel` assembly (multi-target net48 + netstandard2.0, Apache.Arrow 18.0.0). Three pieces:
  - `ArrowMarshal` — C# half of the kernel data plane. `EncodeTable` / `EncodeVector` / `EncodeScalar` / `PeekShape` / `Decode`. Schema metadata (`pyexcel-shape`, `pyexcel-orientation`) matches `arrow_io.py` byte-for-byte. Per-column type inference (double/bool/string with string fallback for mixed), nulls preserved. 23 unit tests.
  - `PythonResolver` + `KernelHost` — discovery (env var → workbook venv → PATH) plus a process-wide `Lazy<KernelClient>` whose first access boots the kernel and whose `Dispose` is the add-in unload hook. Phase 3 will move ownership to per-workbook state.
  - `PyRun.Execute(script, input, kwargs, client, …)` — shared marshal-and-dispatch core for both the planned UDF and the ribbon button. 13 e2e tests through a real Python kernel are the cross-language conformance check for ArrowMarshal ↔ arrow_io.py.
  - Repository housekeeping: previously fragmented `claude/*` branches consolidated into the single `NET_Migration` working branch.
- **2026-05-23 — Phase 2 complete.** Shipped the rest of the kernel data plane in one session, both CI lanes green on `ae0b4f0`:
  - `arrow_io.py` — shape-preserving Arrow IPC for DataFrame / Series / list / tuple / 1-D-or-2-D numpy / scalar, with `pyexcel-shape` and `pyexcel-orientation` schema metadata so the host can reconstruct cell geometry. 39 pytest tests.
  - `worker.py` — pure `run_job(meta, payloads) -> JobOutcome`; loads the user script (mtime-cached), decodes Arrow payloads, calls the target function, replies with `RUN_RESULT` or a typed `ERROR` (9 stable codes: `BadRequest` / `ModuleNotFound` / `ModuleLoadError` / `ModuleExecError` / `FunctionNotFound` / `FunctionNotCallable` / `BadInput` / `BadReturnType` / `Exception`). 23 unit tests + 2 e2e supervisor tests.
  - `PyExcel.Kernel.Client` — new assembly. Typed `RunRequest` / `RunResult` / `KernelException`, `ProgressReceived` / `LogReceived` events, sync `Run` + async `RunAsync`, fire-and-forget `Cancel`. Required a dual-lock refactor of `KernelSupervisor` (`ExchangeSemaphore` + separate read/write locks) so Cancel can fire while a Run is parked in a read. 15 C# integration tests against a real Python subprocess.
  - Windows named-pipe transport — Python `_winapi` client against `\\.\pipe\<name>` with retry on `ERROR_PIPE_BUSY` / `ERROR_FILE_NOT_FOUND`. C# side now sets a DACL granting only the current-user SID at pipe creation (net48 only — netstandard2.0 is Linux-only in CI, so the DACL block is `#if NETFRAMEWORK`).
  - CI: Windows lane now installs Python 3.12 + pyarrow/pandas/numpy and runs the C# integration tests against the Win32 transport. Both lanes green; Phase 2 exit criteria fully met.
- **2026-05-23 — Phase 2 step 3: KernelSupervisor + python entry point.** Added `KernelSupervisor.cs` (C# owns the named-pipe server, spawns `python -m pyexcel.kernel --pipe <name>` via argv, runs HELLO handshake with protocol-version check, exposes `Ping`/`Shutdown`, and force-kills on dispose so no orphaned python.exe). Added `embedded/pyexcel/kernel/{transport.py, supervisor.py, __main__.py}` — POSIX/AF_UNIX client connecting to `/tmp/CoreFxPipe_<name>` (matches .NET's pipe-on-Linux path), supervisor event loop handling HELLO/PING/PONG/SHUTDOWN, ERROR reply for not-yet-supported frames. Tests: 3 C# integration tests (round-trip + 10×PING + dispose-without-shutdown) and 3 pytest tests (handshake roundtrip, protocol-mismatch rejection, ERROR-after-unsupported-frame keeps loop alive). CI workflow reordered so Python is set up before `dotnet test` (integration tests spawn python). Windows transport stub raises `NotImplementedError` — landing alongside the Windows kernel CI slice in a later step.
- **2026-05-23 — Phase 2 step 2: FrameTransport (stream + named-pipe).** Added `FrameTransport.cs` wrapping any `Stream` (MemoryStream for tests, `NamedPipeClientStream` for production) with synchronous `ReadFrame`/`WriteFrame` and a `ConnectNamedPipe` static factory. Added `FrameTransportTests.cs` covering MemoryStream roundtrip, dispose semantics, oversize rejection, and a real Windows-named-pipe/Linux-Unix-domain-socket roundtrip pairing client to in-process server.
- **2026-05-23 — Phase 2 step 1: framing.** Added `PyExcel.Bridge` (multi-target `net48`/`netstandard2.0`) with `Framing.cs` + a stdlib `CanonicalJson` encoder/decoder, mirroring `framing.py` byte-for-byte. Added `PyExcel.Bridge.Tests` (xUnit, net8.0) with the Python test-suite ported 1:1, plus cross-language golden hex vectors (`test_cross_language_vectors.py` ↔ `CrossLanguageVectorsTests.cs`) that pin the on-wire format. CI now builds the netstandard slice on Linux and runs `dotnet test`.
- **2026-05-22 — Phase 0 complete.** Removed the personal-data `__main__` block and trailing dead code from `xmlParsing.py`; replaced the bloated root `requirements.txt` with the minimal v2-kernel set; added `.github/workflows/ci.yml`; confirmed the v1-frozen policy below.
- **2026-05-22 — Planning.** Repo audited; `ARCHITECTURE.md` and this roadmap written; version/phase mismatches reconciled; the four architecture decisions resolved.

**v1 maintenance policy:** v1 (`PyExcel.xlam`) was frozen while the rewrite was built — security and critical-data-loss fixes only, no new features. v1 has now been **removed from the repository** (Phase 9).

## Definition of production-grade (the bar every phase must clear)

A phase delivery is complete only when **all** of these hold for the code it adds:

- [ ] **Tested.** Unit tests land *with* the code; cross-language contracts (framing, Arrow) have conformance tests. No module merged without tests.
- [ ] **No dead code.** No commented-out blocks, no unused helpers, no "OLD" files, no debug-print artifacts.
- [ ] **Errors are real.** No silent swallowing (`On Error Resume Next` / empty `catch`). Every failure is logged via `ILog`; user-facing failures are surfaced with an actionable message.
- [ ] **No resource leaks.** Processes, COM objects, file handles, pipes, timers are deterministically released.
- [ ] **Builds clean.** `TreatWarningsAsErrors` is on — zero warnings. `dotnet build` + `pytest` green in CI.
- [ ] **Documented.** [`ARCHITECTURE.md`](ARCHITECTURE.md) and this file are updated as part of the phase's definition of done.
- [ ] **No hardcoded paths or personal data.** Nothing machine- or author-specific.

## Canonical phase model

This is the **single source of truth** for phase numbering. `docs/v2-build.md`
and the `// PHASE n` comments in the C# defer to it.

| Phase | Title | Headline deliverable |
| --- | --- | --- |
| 1 | Skeleton | `.xll` loads, ribbon tab renders ✅ |
| 2 | Bridge & kernel core | C# ↔ Python talk over the pipe |
| 3 | State & events | Ribbon reflects per-workbook state |
| 4 | Excel marshalling & first run | One real script runs end to end |
| 5 | Data services & shell | Import / Export / Paste work |
| 6 | Charts | Plotly/Matplotlib results render as Excel charts |
| 7 | Setup | Fresh-machine provisioning works |
| 8 | Forms & UI polish | Every dialog is a working WinForm |
| 9 | Cutover & v1 retirement | v2 ships; VBA deleted |

---

## Phase 0 — Reconciliation & cleanup (do before Phase 2)

Make the repo internally consistent and remove what must never reach v2.

- [x] Remove the `panadas==0.2` typosquat from `requirements.txt`.
- [x] Re-encode `src/embedded/requirements.txt` from UTF-16 to UTF-8.
- [x] Reconcile version strings (`README.md`) and phase numbers (`docs/v2-build.md`, C# `// PHASE` comments).
- [x] Add `ARCHITECTURE.md` and this `ROADMAP.md` as the canonical reference docs.
- [x] **Strip personal data** — removed the `__main__` test block and trailing commented-out dead code from `src/embedded/xmlParsing.py` (1941 → 762 lines).
- [x] **Slim dependencies** — root `requirements.txt` is now the minimal v2-kernel set (`pandas numpy pyarrow plotly matplotlib`); the author-specific `git+https` deps are dropped. (v1's `src/embedded/requirements.txt` was removed with the rest of v1 in Phase 9.)
- [x] **v1 maintenance policy** — confirmed frozen: security / critical-data-loss fixes only, no new features (see *Status & progress log*).
- [x] **CI workflow** — `.github/workflows/ci.yml`: Linux builds the `netstandard2.0` slice + runs `pytest`; Windows builds the full solution.

---

## Phase 1 — Skeleton ✅ (complete)

Delivered: `PyExcel.Common` (logging), `PyExcel.Ribbon` skeleton, `PyExcel.Addin`
skeleton, `embedded/pyexcel/kernel/framing.py` + tests. The `.xll` loads, the
Python tab renders, every button is a logged stub.

---

## Phase 2 — Bridge & kernel core

**Objective.** C# and the Python kernel exchange frames over a named pipe; the
kernel's lifetime is owned and deterministic.

**Deliverables.** `PyExcel.Bridge`, `PyExcel.Kernel.Client`, and the Python
`pyexcel.kernel` package (`supervisor.py`, `worker.py`, `arrow_io.py`, `__main__.py`).

- [x] `PyExcel.Bridge/Framing.cs` — mirror `framing.py` byte-for-byte (same frame layout, bounds, determinism).
- [x] Cross-language conformance tests: frames encoded in C# decode in Python and vice versa (golden hex vectors pin the wire format on both sides).
- [x] Bounded/malformed-frame handling on the C# side (mirror the `framing.py` test suite).
- [x] Named-pipe transport — POSIX side (C# `NamedPipeServerStream` ↔ Python `socket(AF_UNIX)` against `/tmp/CoreFxPipe_<name>`) and Windows side (C# named pipe with a DACL granting only the current-user SID ↔ Python `_winapi` client against `\\.\pipe\<name>`). Wrong-user processes get `ERROR_ACCESS_DENIED` at connect time before any frame bytes cross the boundary.
- [x] `KernelSupervisor` — spawns `python -m pyexcel.kernel` via argv (no shell), `HELLO` handshake with protocol-version check, `Ping`/`Shutdown` API, deterministic kill on `Dispose`. PING/PONG health-check loop on a background timer is a separate item.
- [x] `PyExcel.Kernel.Client` — typed API: `RunRequest`/`RunResult`/`KernelException`, `ProgressReceived`/`LogReceived` events, sync `Run` + async `RunAsync`, and a fire-and-forget `Cancel` that uses only the write lock so it can fire while a `Run` is parked in a read. Requires a dual-lock refactor of `KernelSupervisor` (exchange semaphore + separate read/write locks) — done in the same change.
- [x] Python `supervisor.py` — connects to the pipe, runs the HELLO/PING/PONG/SHUTDOWN loop. Worker dispatch is a follow-up.
- [x] Python `worker.py` — run one job: receive `RUN_REQUEST` → load the user script (mtime-cached) → decode Arrow payloads → call the target function → reply with `RUN_RESULT` or a typed `ERROR` (`BadRequest` / `ModuleNotFound` / `ModuleExecError` / `FunctionNotFound` / `FunctionNotCallable` / `BadInput` / `BadReturnType` / `Exception`). Pure function; supervisor wires it into the dispatch loop.
- [x] Python `arrow_io.py` — DataFrame / list / scalar ↔ Arrow IPC stream. Shape (`table`/`vector`/`scalar`) and vector orientation are carried as Arrow schema metadata so the host can spill back into the right cell geometry.

**Exit criteria.** C# spawns the kernel, completes a `HELLO`/`PING`/`PONG`
round-trip, and kills it cleanly on shutdown. Framing conformance tests pass in
both languages. No orphaned `python.exe` after Excel closes.

---

## Phase 3 — State & events

**Objective.** The ribbon accurately reflects per-workbook state; switching
sheets/workbooks never shows stale values.

**Deliverables.** `PyExcel.State`, an `AppEventSink`.

- [x] `StateService` — new `PyExcel.State` assembly. Immutable `WorkbookState` records keyed by workbook id; thread-safe `Get` / `Update` / typed setters (`SetEnabled`, `SetSelectedScript`, `AddAction`, `DeleteAction`, …) plus a `StateChanged` event. `IWorkbookContext` abstraction over `Application.ActiveWorkbook`, with a `NullWorkbookContext` default so the ribbon renders sanely before `AutoOpen` wires the real one. Pure-logic; tested cross-platform.
- [x] Persist per-workbook state as a `CustomXMLPart` on the workbook. `WorkbookStatePersister` (net48, COM) writes on `WorkbookBeforeSave` / reads on `WorkbookOpen`, delegating the XML to the tested `WorkbookStateCodec`. Code-complete; runtime behaviour pending the Windows smoke test (`docs/phase3-and-4-completion.md` §3).
- [x] `AppEventSink` — `WorkbookOpen` / `Activate` / `BeforeSave` / `BeforeClose` / `SheetActivate` → update state + invalidate ribbon. Typed Application events via the embedded `ExcelDna.Interop` PIA (CI-restorable, no runtime dependency). Code-complete; runtime behaviour pending the Windows smoke test (`docs/phase3-and-4-completion.md` §2).
- [x] Wire `RibbonEnabled` / all getters to `StateService`. Every former-hardcoded callback in `PyExcelRibbon` now reads from `PyExcelServices.State.Get(activeKey)`. `OnPyInputChange` / `OnPyOutputChange` / `OnScriptChange` / `OnActionChange` write back through `StateService`. Ribbon redraw queued via `ExcelAsyncUtil.QueueAsMacro` so `StateChanged` from a worker thread is safe.
- [x] `FileSystemWatcher` on `userScripts/` — new `ScriptDirectoryWatcher` watches a directory for `.py` files and pushes a sorted, deduped list to a callback (typically wired to `StateService.SetAvailableScripts`). Initial snapshot fires inside the constructor so consumers start with the current contents. Tested cross-platform with `tmp` directories.
- [x] Wire `OnAddAction` / `OnEditAction` / `OnDeleteAction`. **`OnDeleteAction`** routes to `StateService.DeleteAction` against the selected action. **`OnAddAction` / `OnEditAction`** (Phase 8) now show the WinForms `EditActionForm` and route its `RibbonAction` result through `StateService.AddAction` (upsert; Edit drops the old entry first on a rename) — the state plumbing was ready since Phase 3, the form landed in Phase 8.

**Exit criteria.** Enabling a workbook lights the ribbon; switching sheet or
workbook updates every ribbon field correctly; state survives a save/reopen.

---

## Phase 4 — Excel marshalling & first run (the thin slice)

> **Handoff brief:** [`docs/phase4-handoff.md`](docs/phase4-handoff.md) —
> what Phase 2 shipped (public APIs, wire contract, known gaps) and what
> Phase 4 still has to build (C#-side Arrow encoder, `=PY.RUN` UDF,
> short-term kernel lifecycle). Read that first.

**Objective.** Prove one full run works end to end — this de-risks the pipe,
Arrow, COM interop, and the threading model in a single slice before going wide.

**Deliverables.** `PyExcel.Excel`; a wired `OnRunPython`.

- [x] Range → Arrow: read `object?[,]` / `object?[]` / scalar into Arrow IPC streams with shape metadata (`pyexcel-shape`, `pyexcel-orientation`) matching `arrow_io.py`. Per-column type inference; mixed columns fall back to string. Date/Excel-error handling still ahead.
- [x] Arrow → Range: decode Arrow IPC back to `object?[,]` / `object?[]` / scalar. Defaults to table for buffers without PyExcel metadata (interoperable with external Arrow writers).
- [x] `PyRun.Execute` — shared marshal-and-dispatch core for both the `=PY.RUN` UDF and the ribbon `OnRunPython` button. Resolves the script path (relative → workbook dir), encodes input as Arrow, calls `KernelClient.Run`, decodes the response (honours `pyexcel-orientation` to spill row vs column), and surfaces a `None` return as a sentinel the wrapper translates to `ExcelEmpty`. No Excel-DNA dependency — runs in netstandard2.0, fully unit-testable.
- [x] `KernelHost` — process-wide `Lazy<KernelClient>` lifecycle wrapper for Phase 4. First `Client` access boots the kernel; idempotent `Dispose` for the add-in unload hook. Phase 3 will replace this with per-workbook ownership in `StateService`.
- [x] `PythonResolver` — three-tier discovery: `PYEXCEL_PYTHON` env var → `<workbook>/.pyexcel-venv/{Scripts,bin}/python` → PATH fallback. Plus `ResolveEmbeddedPath()` walking up from `AppContext.BaseDirectory` to find `embedded/pyexcel/kernel/__main__.py`.
- [x] Parse the Input/Output ribbon fields, including the `{name}=Range` syntax. `PyExcel.State.RibbonRangeParser.Parse` returns an ordered list of `RangeBinding(Name?, RangeText)` records, handling anonymous bindings (`A1:C10`), named bindings (`prices=A1:C10`), and the semicolon-separated multi-binding syntax (`prices=A1:C10; signals=D1:D10`). 15 unit tests; consumed by `RangeRunner` to dispatch multi-input `PyRun.ExecuteMany` from the ribbon button.
- [x] `=PY.RUN(script, input, [function])` Excel-DNA UDF (net48-only) — wrapper around `PyRun.Execute` that translates Excel-DNA sentinel args (`ExcelMissing`/`ExcelEmpty`/`ExcelError`) to plain .NET shapes and threads errors back as `#VALUE!` with the full Python traceback logged to `Trace`.
- [x] **SAFE-1** for the UDF: dispatched through `ExcelAsyncUtil.Observe`, so the calc thread never blocks. First call returns `#N/A` immediately; the cell auto-refreshes when the kernel returns. (The earlier slice used `ExcelAsyncUtil.Run`; the switch to `Observe` is what enables the UDF cancel bridge below.)
- [x] Kernel-side cooperative CANCEL — supervisor dispatches `RUN_REQUEST` on a worker thread and pumps inbound frames during the run, so `CANCEL` sets a flag the user's `transform()` can read via `pyexcel.kernel.is_cancelled()`. `PING` continues to answer for liveness. The kernel replies `ERROR / Cancelled` whether the user code noticed and aborted or completed naturally. **Now also wired through the UDF:** the `=PY.RUN` observable cancels its `CancellationTokenSource` on dispose; the token registration inside `KernelClient.RunAsync` pushes the `CANCEL` frame, so Excel-DNA's cancel-on-formula-change actually aborts the run. See `docs/phase3-and-4-completion.md` §4.
- [x] `OnRunPython` ribbon button handler (same dispatch core as the UDF, called from the button event), non-blocking SAFE-1 pattern. Shipped + verified on real Excel 2026-05-29 (see status log / `docs/phase3-and-4-completion.md` §7). Progress UI is the item below.
- [x] Kernel-side progress: `pyexcel.kernel.report_progress(percent, message)` emits `PROGRESS` frames the supervisor streams to the host (`KernelClient.ProgressReceived` already wired). 6 tests; 146/146 kernel tests green.
- [x] Non-blocking progress UI (WinForms) rendering those `PROGRESS` frames, with a working **Cancel**. Shipped in Phase 8: the modeless `ProgressForm` subscribes to `KernelClient.ProgressReceived` (forwarded by `RangeRunner`) and its Cancel button trips the `CancellationToken` threaded into `ExecuteManyAsync`, which the kernel turns into a `CANCEL` frame.
- [x] Archive a run (inputs, outputs, log); retention cap. `PyExcel.State.RunArchive` writes each run to `%LOCALAPPDATA%\PyExcel\runs\{yyyyMMddTHHmmssfff}_{rand}\` — Arrow IPC bytes for every input (`input_N.arrow`), the output (`output.arrow`, present iff the run produced a payload), a human-readable manifest (`manifest.txt`, line-per-field), and the formatted error block (`error.txt`, present on Error / Cancelled). Retention defaults to 20 most-recent runs; older directories are evicted FIFO after each write. Threaded through both surfaces via a new optional `RunArchiveContext` parameter on `PyRun.Execute*` — `PyRunFunction` tags its runs `"PY.RUN"`, `RangeRunner` tags them `"Run Python button"`. Best-effort: an I/O fault inside the archive can't mask the user-facing result or the in-flight exception.
- [x] Surface kernel errors — `KernelException` and host-side faults now flow into a per-workbook `ErrorService` (with a global fallback slot for failures that arrive before any workbook is bound). The ribbon adds an **Errors** group with **Show Last Error** (opens Excel-DNA's `LogDisplay` with the formatted traceback) and **Copy Last Error** (puts the same block on the clipboard for bug reports). Both buttons enable only when there's something to show — `RibbonHasError` + `ErrorService.ErrorChanged` repaint hook. Cell still shows `#VALUE!` so `ISERROR()` keeps working. *(Two UI niceties — a per-script error pane and a hover tooltip on the failing cell — were floated as "Phase 8 UI work" but were never part of Phase 8's nine-dialog charter; they're optional future polish, not a blocker for this deliverable, which is satisfied.)*
- [x] `ExcelFormula` round-trip (A1 mode) — marshalling layer only. Python `pyexcel.kernel.Formula("=…")` ↔ C# `PyExcel.Excel.Formula("=…")`. Wire format: a string Arrow column with field-level metadata `pyexcel-cell-type=formula`. Scalar + vector are wired; DataFrame-with-formula-columns is skipped (encode side flattens to plain strings — documented gap). The RangeRunner write hook (`range.Formula = …` vs `range.Value2 = …`) is a separate ribbon-integration follow-up.
- [x] Tests: range ↔ Arrow round-trip incl. dates, nulls, formulas. **Note:** writing the date tests surfaced and fixed a real decode bug — `ArrowMarshal.ReadCell` was falling through to `_ => array.GetType().Name` for `Date32Array` / `Date64Array` / `TimestampArray`, so a Python script returning a `datetime.datetime` rendered the literal string `"TimestampArray"` in Excel. The decoder now lands on a naive `DateTime` (matching pyarrow's no-timezone default for `datetime` / `date`).

**Exit criteria.** A user `transform()` taking one input table and returning one
table runs from a ribbon click and writes results; a failing script shows a
clear, actionable error.

---

## Phase 5 — Data services & shell

**Objective.** Import, Export, and Paste work correctly for the documented formats.

**Deliverables.** Import/Export/Paste services in `PyExcel.Excel`; `PyExcel.Common.Shell`.

- [x] CSV/TSV import via a real **RFC-4180** parser — embedded newlines/commas/quotes, UTF-8 + BOM detection, correct delimiter for `.tsv`. Parser foundation (`CsvParser`) lands the wire format; the COM-bound `ImportService` builds an `ImportPlan` via `ImportPlanner` (path resolution against the workbook dir, delimiter detection from extension, validation), parses on a background `Task`, then queues a main-thread `Resize`+`Value2` write through `ExcelAsyncUtil.QueueAsMacro`. Per-cell type inference (`CsvCellTypeInference`) matches Excel's import: bool tokens → bool, invariant-culture parseable → double, leading-zero / leading-plus → string, otherwise → string.
- [x] Excel-format import (XLSX/XLSM/XLSB) via COM. `ImportPlanner.DetectFormat` routes Excel binaries to the COM importer; `ImportPlanner.ParsePathAndSheet` splits the user-typed input on a trailing `!SheetName` (only when the path before is an Excel extension, so a CSV with `!` in its filename is left alone). `ImportService.RunExcelImport` queues a single `QueueAsMacro` that saves/restores `ScreenUpdating` + `DisplayAlerts`, reuses an already-open workbook by `FullName` match (no close — leaves the user's view alone), or opens fresh with `UpdateLinks=0, ReadOnly=true` and closes on the way out; sheet defaults to `Sheets[1]` when not specified, otherwise looks up by name with a clean `FormatException` for missing names. `UsedRange.Value2` is rebased from Excel's 1-based array and ExcelDna sentinels (`ExcelEmpty` / `ExcelMissing` / `ExcelError`) are mapped to `null`. The `path!Sheet` syntax is the keyboard interim; the **WinForms sheet picker shipped in Phase 8** — when an Excel path is given without a pinned sheet and the workbook has several, `ImportService` enumerates them and prompts via `SheetPickerForm` (`SheetSelection.Resolve` drives the prompt-or-resolve decision). `.xls` (legacy binary) and `.ods` (OpenDocument) are explicitly rejected with actionable error messages — neither is in the v2 supported set.
- [x] Export ranges to CSV/Excel with correct quoting and explicit encoding. Writer foundation (`CsvWriter`) handles the quoting; the COM-bound `ExportService` builds an `ExportPlan` via `ExportPlanner`, reads `Range.Value2` on the main thread (stringifying each cell through `CsvCellFormatter` — invariant-culture round-trip for doubles, `TRUE`/`FALSE` for bools matching the import-side recogniser, ISO-8601 for `DateTime`s, Excel-DNA sentinel stripping), then writes UTF-8 to disk on a background `Task` via `FileStream` + `CsvWriter.Write(Stream, …)`.
- [x] Paste a saved artifact to a range, with overwrite confirmation. `PastePlanner` picks the newest output-bearing archived run, preferring same-workbook runs over unbound ones, falling back when the active workbook has none. `PasteService` decodes `output.arrow` via `ArrowMarshal` on a background task and writes back via `QueueAsMacro` — same SAFE-1 contract as the other Phase 5 services. **Overwrite confirmation:** before writing, the service reads the target range's current `Value2`, strips Excel-DNA's `ExcelEmpty` / `ExcelMissing` sentinels, and calls cross-platform `PastePreflight.RangeHasContent`. If the range is occupied, `System.Windows.Forms.MessageBox` prompts the user (default button is "No" — accidental Enter doesn't overwrite); a "No" answer logs the cancellation and aborts the paste without touching the sheet. The `PastePreflight.Footprint` helper also drives the resize-then-write so the prompt names the exact footprint the paste will clobber.
- [x] `PyExcel.Common.Shell` — open Explorer, open the user's editor, open the readme; wire `OnOpenExplorer` / `OnReadMe` / `OnEditPython`. `ShellLauncher.Open(path)` shells the OS default handler via `Process.Start` with `UseShellExecute=true`; `ShellLauncher.OpenInExplorer(path)` uses Explorer's `/select,` verb for files and a plain open for directories. Net48-only (the `UseShellExecute=true` semantics aren't portable across .NET cores). Ribbon wiring: `OnOpenExplorer` opens the active workbook's directory (no-op + LogDisplay note for an unsaved workbook); `OnReadMe` opens `<workbookDir>/README.md` if present, falling back to the in-Excel alert; `OnEditPython` opens `<workbookDir>/userScripts/<selectedScript>` if both the script selection and workbook directory exist.
- [x] Wire the Import / Export / Paste ribbon groups end to end. All three buttons routed through their respective services; Paste's run-archive lookup means it Just Works after any `=PY.RUN` or Run-Python-button run that produced output.
- [x] Tests for CSV edge cases — 38 parser test methods (42 cases counting `[Theory]` rows) + 28 writer test methods (30 cases) in `CsvParserTests` / `CsvWriterTests`. Covers all line endings, BOM stripping, quoted fields with embedded commas / quotes / newlines / CRLF, escaped quotes (`""`), permissive bare-quotes-in-unquoted-fields, content-after-closing-quote concatenation, blank-line-as-empty-record semantics, trailing-newline non-emission, leading/trailing/consecutive empty fields, whitespace preservation, alt delimiters (tab, semicolon), invalid-delimiter validation, null-input guards, stream overloads (UTF-8, BOM bytes, leave-open), and a parser↔writer round-trip on tricky payloads (commas, quotes, embedded newlines, leading-trailing whitespace).

**Exit criteria.** Import/export/paste handle the documented formats with correct
encoding and quoting; no data corruption on round-trip.

---

## Phase 6 — Charts

**Objective.** A `transform()` that returns a figure produces a native Excel chart.

**Deliverables.** `PyExcel.ChartBuilder`; chart support in the kernel.

- [x] Kernel: Plotly figure → a JSON **chart spec** (port the `PlotlyToExcelXMLConverter` traversal; emit JSON, not XML — carried in the `RUN_RESULT` frame). *(`pyexcel.kernel.chart.plotly_figure_to_spec`; new `chart` Arrow shape.)*
- [x] Kernel: Matplotlib figure → image artifact (SVG, PNG fallback). *(`matplotlib_figure_to_image`; new `image` Arrow shape with `pyexcel-image-format` field metadata.)*
- [x] `PyExcel.ChartBuilder`: JSON chart spec → native Excel chart (port the live chart-COM logic from `chartBuilder.bas`). *(Landed as `PyExcel.Excel/ChartBuilder.cs` — same assembly as the other COM services rather than a separate project; `ChartSpecParser` + `ChartTraceData` + `ChartColor` are the cross-platform halves.)*
- [x] Guard against orphan charts (clean up on failure) and missing spec attributes. *(ChartObject deleted on any build failure; parser throws `FormatException` naming each missing/malformed required attribute and defaults the optional ones.)*
- [x] Support the documented chart types; explicit, surfaced handling of unsupported types. *(scatter / scatter_lines / scatter_lines_markers / line / column / bar / area / pie / histogram / bubble; box / violin / heatmap / waterfall etc. raise `UnsupportedChartTypeError` naming the type and the supported set — surfaced as `BadReturnType`.)*
- [x] Tests for the chart-spec contract. *(75 Python `test_chart.py` + 38 `ChartSpecParserTests` + 25 `ChartTraceDataTests` + 24 `ChartColorTests` + 10 `ChartWireTests` + 3 e2e `PyRunTests` chart round-trips.)*

**Exit criteria.** A Plotly figure renders as a native Excel chart; a Matplotlib
figure embeds as an image; a malformed spec fails cleanly with a message.

---

## Phase 7 — Setup

**Objective.** A fresh machine can be provisioned reliably, with diagnosable failures.

**Deliverables.** `PyExcel.Setup`.

- [ ] First-run wizard — project folder, convert host to `.xlsm`, create the project tree. *(Still open — the headless `SetupService` engine (venv, pip, kernel extraction, dependency verification) is built and tested, but it is **wired to nothing**: no ribbon button, no boot-time hook. The wizard UI + wiring is the one remaining bootstrap task before a freshly-downloaded `.xll` can self-provision its Python environment. It was floated as "Phase 8 WinForms" but is not one of Phase 8's nine charter dialogs, so it does not gate Phase 8's completion — it's the immediate follow-up.)*
- [x] venv creation against system Python; detect a missing interpreter or the Windows Store stub and say so. *(`VenvProvisioner` + `SystemPythonProbe` — Windows-Store stub heuristic by location + reparse-point + size.)*
- [x] Extract the kernel package from **.NET embedded resources** (no base64 sheet, no chunk-assembly). *(`KernelResourceExtractor` over `<EmbeddedResource>` items in `PyExcel.Setup.csproj`; idempotent with byte-equality skip.)*
- [x] `pip install` the canonical requirements; capture stdout+stderr to a real log; surface failures. *(`PipRunner` via `ProcessRunner`; each output line forwarded to `ILog` as it arrives, full streams returned on the result.)*
- [x] Dependency verification with a clear pass/fail (no silent 80% threshold). *(`DependencyVerifier`: every required module must import via `python -c "import X"`; per-module status returned.)*
- [x] Path resolution that handles UNC (`\\server\share`) and localized / non-default SharePoint libraries. *(`ProjectPathResolver`: `\\?\UNC\` normalisation, OneDrive/Consumer/Commercial classification with directory-boundary safety.)*
- [ ] Retire `Update.bas` — updating = shipping a new `.xll` (Excel-DNA reloads it). *(Sliced into Phase 9 alongside the rest of `src/module/` removal.)*

**Exit criteria.** Clean setup on a fresh Windows machine; every failure mode
produces a specific, actionable message; UNC and SharePoint project paths work.

---

## Phase 8 — Forms & UI polish

**Objective.** Every ribbon action that needs a dialog has a working one.

**Deliverables.** `PyExcel.Forms` (WinForms).

- [x] Rewrite the 9 dialogs as WinForms: range picker, edit action/import/export/paste, export wizard, orientation, sheet picker, progress. **All nine shipped** in `PyExcel.Forms` (multi-target net48 + netstandard2.0), each a cross-platform validator (Linux-tested) + a net48 WinForms shell + ribbon wiring: EditAction (`EditActionValidator`/`KwargsText`), sheet picker (`SheetPickerValidator`/`SheetSelection`), Edit-Import/Export/Paste (`EditIoValidator` reusing the planners, one `EditIoForm`), range picker (`RangeAddressValidator` + `RangePickerForm`, embedded as "Pick…" on every range field), orientation (`OrientationResolver` + `OrientationForm`, wired into `PasteService`), export wizard (`ExportBatchValidator` + `ExportWizardForm` + `ExportService.RunBatch`), progress (`IRunProgressSink`/`ProgressModel` + modeless `ProgressForm`, wired into `RangeRunner`).
- [x] Wire **every** control — fix the dead `frmExportWizard` row buttons. The wizard's rows live in a `FlowLayoutPanel`; Add/Remove reflow automatically, so the v1 dead-button bug can't recur.
- [x] Replace the off-screen `(-20000,-20000)` form-hide hack with proper modal/owner handling. Every dialog is modal (`ShowDialog(owner)`) or modeless (`Show(owner)`) with Excel's window as owner; nothing is hidden off-screen.
- [x] Input validation in every dialog; clear inline error messaging. Each dialog validates on OK through its cross-platform validator and shows the message inline, only closing when valid.
- [x] Ship the ribbon logo PNG as an embedded resource (`LoadImage`). `customLogo.png` is an `<EmbeddedResource>` on `PyExcel.Ribbon`; `LoadImage("customLogo")` decodes it to a `Bitmap`.

**Exit criteria.** No unwired controls; no dialog can be lost off-screen; invalid
input is caught in the dialog, not downstream.

---

## Phase 9 — Cutover & v1 retirement

**Objective.** v2 becomes the product; v1 is removed.

- [ ] Feature-parity checklist vs v1 — every documented capability verified in v2.
- [~] v1 → v2 per-workbook state migration (or a documented re-enable path). **Pure converter code-complete** — `LegacyStateConverter` (in `PyExcel.State`) maps the raw v1 defined-Name values for one sheet (`PyExcelEnabled` / `SelectedAction` / `Actions` / `cmbScript` / `txtPy*` / `txtImport*` / `txtExport*` / `txtPaste*` — keys pinned in `LegacyStateConverter.LegacyNames`) onto a `WorkbookState` the codec can persist. `ParseActions` is a faithful port of v1's `LoadActionsForSheet` (Chr(29)→Chr(10)→`;` separator auto-detection, named `key=value` vs legacy positional, repeated `input=`/`output=` accumulating with `"; "`, first-wins on duplicate names, `entireRow`/`entreToEnd` designed out). The COM driver (read Names off a live workbook, write the CustomXMLPart) is the Windows tail. Open: the per-sheet→per-workbook collapse policy is left to the driver (no lossless multi-sheet mapping).
- [ ] End-to-end QA on Excel 2016 / 2019 / 365 (x64).
- [x] Delete `src/module/`, `src/embedded/`, `src/Ribbon/`, `PyExcel.xlam`. *(Ribbon logo `customLogo.png` relocated into `PyExcel.Ribbon/Resources/` first — the v2 ribbon embeds it.)*
- [ ] Rewrite `README.md` to describe v2 as the product.
- [ ] Tag the release; update `Directory.Build.props` version off `-alpha`.
- [ ] **Update mechanism (open — placeholder shipped).** "Enable" now also installs/sets up a workbook — install + enable are one button, greyed once the workbook is enabled (Note 3, `OnEnablePyExcel` + `RibbonNotEnabled`). A separate **Update** path is only stubbed: the ribbon's Update button is a disabled placeholder (`RibbonUpdateAvailable` returns false; `OnUpdate` only logs). To finish: decide how the bundled build version is compared against a workbook's provisioned version, add a launch-time "is a newer build available?" check that drives `RibbonUpdateAvailable`, and define what Update re-runs (refresh the extracted kernel + re-sync deps via the idempotent `SetupService`).

**Exit criteria.** v2 ships; no VBA remains in the repository.

---

## Bug disposition

The full audit, classified for the migration.

### Designed out — the v2 architecture removes these; do not port

Locale-dependent number text · `CLng` integer overflow · Excel-epoch/date
heuristics · column/scalar type-inference mistakes · `meta.xml` polling hangs,
heartbeat false-stalls, run-id mismatch · orphaned `python.exe` · `cmd /c`
quoting/injection · multi-chunk base64 extraction corruption · UTF-16
`requirements.txt` · last-sheet-row format clobber.

### Fix in transit — must be done deliberately during the port

- [x] CSV parsing — adopt a real RFC-4180 library (Phase 5). *(`PyExcel.Excel.CsvParser` / `CsvWriter` — done.)*
- [ ] Silent failure — `ILog` + surfaced errors everywhere (every phase; enforced by the production-grade bar).
- [ ] Stale sheet state — single source of truth in `PyExcel.State` (Phase 3).
- [ ] Subprocess hardening — `KernelSupervisor` owns lifetime (Phase 2).
- [x] `frmExportWizard` dead buttons — fixed in the Phase 8 WinForms rewrite: `ExportWizardForm` keeps rows in a `FlowLayoutPanel`, so Add/Remove reflow automatically (no manual repositioning to get wrong).
- [ ] chartBuilder orphan charts / null-attribute crashes — guard in `PyExcel.ChartBuilder` (Phase 6).
- [x] Destructive paste — add overwrite confirmation (Phase 5). *(`PastePreflight.RangeHasContent` + WinForms `MessageBox` in `PasteService` — done.)*
- [ ] Setup diagnostics — surface venv/pip output (Phase 7).

### Fix now — handled in Phase 0

Typosquat removal ✅ · UTF-16 requirements ✅ · dependency slimming ✅ · personal-data removal ✅

## Cut list — delete, do not migrate (~3,000+ lines)

- [ ] `xmlParsing.bas` — entire file (Arrow replaces it).
- [ ] `chartBuilder.bas` lines ~2–1010 — dead alternate engine.
- [ ] `tools.py` — ~1,200 commented-out legacy lines.
- [x] `xmlParsing.py` — ~485 commented lines + the ~570-line `__main__` block. *(done — Phase 0)*
- [ ] `frmEditActionOLD.frm/.frx`; commented blocks in the three `frmEdit*` forms; the `CAppEvents.cls` duplicate.
- [ ] `Import.bas ReadExcel_ADO` (~100 dead lines); unused helpers across modules.
- [ ] Most of `Update.bas` (SmartClean / manifest / version-name machinery).
- [x] `PyExcel.xlam` and all of `src/module/` — removed in Phase 9.

## Cross-cutting — CI & testing

- [ ] Add `.github/workflows/` — on every push: build the `PyExcel.Common` netstandard2.0 slice + run `pytest tests/` (Linux); build the full solution on a Windows runner.
- [ ] Gate merges on green CI.
- [ ] Every phase lands with tests; track coverage of `PyExcel.Excel`, `PyExcel.Bridge`, and the kernel.
- [ ] Code review on every PR against the *Definition of production-grade*.

## Decisions (resolved)

These were open; now settled. Recorded here as the source of truth.

1. **Chart transport — JSON chart spec.** The kernel converts Plotly figures to a JSON chart spec carried in the `RUN_RESULT` frame; `PyExcel.ChartBuilder` builds the native Excel chart from it. No XML layer. *(Phase 6)*
2. **List/scalar transport — everything as Arrow.** Lists become 1-column Arrow batches and scalars 1×1 batches — one uniform marshalling path with full type fidelity, including timestamps. *(Phase 2 `arrow_io.py`, Phase 4)*
3. **State storage — `CustomXMLPart`.** Per-workbook state is stored as a workbook-attached `CustomXMLPart`: invisible, no length limits, no Name Manager clutter. Phase 9 needs a converter that reads v1's defined Names and writes the new part. *(Phase 3, Phase 9)*
4. **Forms UI — WinForms.** The 9 dialogs are rebuilt as WinForms — lowest hosting friction with Excel-DNA, near 1:1 with the existing layouts. *(Phase 8)*

## Phase 6 chart transfer — open hardening notes

Robustness assessment of the Phase 6 chart pipeline as it stands. The
contract layer (figure → spec → JSON → Arrow → typed `ChartSpec` →
parsed document) is pinned by ~170 tests on both sides and is robust
against malformed/hostile specs and unsupported figure types (both
surface clean errors that name the offending element). The items below
are the residual gaps a Windows + Excel session needs to close.

- [ ] **COM render is unverified.** `ChartBuilder.cs` compiles clean on both target frameworks but has never executed against a real Excel instance. The COM calls are late-bound `dynamic`, so a wrong member name or hand-entered enum constant (`xlXYScatter = -4169` and ~25 friends) only surfaces at runtime. Same posture as every Phase 3–5 COM tail; the Windows smoke test (a Plotly figure renders, a Matplotlib figure embeds, a malformed spec fails clean with no orphan ChartObject) is the gating verification.
- [ ] **Cosmetic styling degrades quietly.** Colours / dashes / markers / per-point labels are individually `try`-guarded so a rejected COM call logs to `Trace` and the chart still appears, possibly less styled. This is deliberate (a missed colour must not cost the user the chart) but means "chart looks slightly off" doesn't throw. A user-facing toggle to surface cosmetic failures (or a per-run summary in `LogDisplay` listing what was skipped) would close this.
- [ ] **No point-count guard.** Excel's series cap is ~32k points; a million-point scatter would produce a huge JSON spec and a COM rejection at `Series.Values =` assignment time. Failure mode is correct (surfaced error, no corruption) but the diagnostic isn't friendly. Add an early limit in `ChartSpecParser` (configurable, default 32k) that throws `FormatException` with a clear "too many points; reduce or aggregate upstream" message.
- [ ] **SVG embedding assumes Excel 2016+.** Pre-2016 Excel rejects SVG in `Shapes.AddPicture`; today the error surfaces to `LogDisplay` but the picture doesn't land. The kernel-side PNG fallback only triggers when SVG *rendering* fails on the Python side, not when *embedding* fails on the host side. Either detect the Excel version up-front and request PNG from the kernel, or fall back to a host-side rasteriser when `AddPicture(svg, …)` throws.

## Future development — chart support expansion

Plotly is broad; the Phase 6 port covers the v1 subset of trace types
plus a few Phase-6-era additions (bubble auto-detection, `scatter+fill`
→ area, pie via `labels`/`values`). The list below is what a user
following the Plotly docs might reach for that the v2 chart pipeline
currently does not render — none of these are bugs in what's shipped;
they are scope to expand when there is a clear user case.

- [ ] **Subplots / facets.** A `plotly.subplots.make_subplots` figure renders only the first subplot's traces against a single chart area. Native Excel has no equivalent multi-pane container; the realistic path is to emit one `ChartObject` per subplot tiled in a grid (output range divided into N×M cells). Requires a new `subplots` block in the spec schema.
- [ ] **Multiple secondary axes.** Anything beyond `yaxis="y2"` (so `y3`, `y4`, …) is collapsed into the single `secondary` axis group. Excel itself only natively supports one secondary y-axis per chart group, so this is partly a fundamental limit, but the parser could at least surface a warning instead of silently merging.
- [ ] **Per-point colour arrays.** A `marker=dict(color=[…per point colors…])` is currently reduced to a single representative colour for the series. Rendering per-point colour needs a `Points(i).Format.Fill` loop in `ChartBuilder` and a per-point colour array on the spec (currently only `pt[].style.color` exists for annotations, not data points).
- [ ] **Area `stackgroup`.** Plotly's stacked-area mode (`stackgroup="one"`) currently lands as overlapping area series rather than a true stacked area. The series-type map already handles `area_stacked` for the explicit case; wiring `stackgroup` detection into `_series_type_for` would close it.
- [ ] **Horizontal histogram.** `go.Histogram(y=...)` (a horizontal histogram) currently yields no data because the binning path reads `trace.x` only. Either bin `y` symmetrically and swap axes, or surface "horizontal histogram not supported" instead of an empty trace.
- [ ] **Box / violin / heatmap / waterfall / candlestick / OHLC.** Today these raise `UnsupportedChartTypeError` with a clear message — the right behaviour for "we don't render this". A future slice could add native renders for the subset Excel supports directly (Excel has native box plot, waterfall, and histogram chart types since 2016; `xlBoxwhisker = 121`, `xlWaterfall = 119` already exist in the COM enum), gated on user demand.
- [ ] **3-D charts.** `scatter3d` / `surface` / `mesh3d` — Excel's 3-D chart types are weak, and the v1 add-in never supported these. Probably permanent "no".
- [ ] **Animation frames.** Plotly's `frames` (sliders, play button) have no Excel counterpart. Permanent "no".


