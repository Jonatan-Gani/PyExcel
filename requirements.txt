# PyExcel v2 — Python kernel base dependencies.
#
# The minimal set the v2 kernel framework (pyexcel.kernel) needs. PyExcel.Setup
# installs it into the project virtual environment; users add their own packages
# on top for their transform scripts.
#
# Versions are intentionally unpinned until the kernel is built and tested
# (see ROADMAP.md, Phase 7 — Setup), at which point this becomes a pinned lock
# file. The v1 add-in installs src/embedded/requirements.txt instead.
pandas
numpy
pyarrow
plotly
matplotlib
